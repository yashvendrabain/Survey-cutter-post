Apply Codex's two updated files:

src/excel_exporter.py
tests/test_excel_exporter.py

The _short_label_for_question now uses a three-tier priority: explicit caller-provided labels → contiguous-substring AI labels → fall back to question_text. The path fix tries the simple repo-root layout first and falls back to the nested Replit layout — so it works in both environments without local edits.
Verification — full sequence, no holds:

Run the full test suite:
bashpython -m pytest tests/ -v 2>&1 | tail -30
Expect: 457 passing, 0 failing.
Run the engine on BCN sample:
bashpython -c "
from pathlib import Path
from src.io import load_inputs
from src.datamap_parser import parse_datamap
from src.question_classifier import classify_questions
from src.single_cut.engine import compute_single_cuts
from src.calculation_log import CalculationLog
from src.excel_exporter import export_single_cuts

raw_path = Path('sample_data/BCN_LTB_raw_data.xlsx')
map_path = Path('sample_data/BCN_LTB_datamap.xlsx')

decoded_df, data_map, _report = load_inputs(raw_path, map_path)
schema = classify_questions(data_map, set(decoded_df.columns))
log = CalculationLog()
results, skips = compute_single_cuts(schema, decoded_df, log)
print(f'Results: {len(results)}, Skips: {len(skips)}')
print(f'Result types: {sorted(set(type(r).__name__ for r in results))}')

export_single_cuts(results=results, schema=schema, log=log, output_path=Path('/tmp/bcn_test_output.xlsx'))
print('Exported to /tmp/bcn_test_output.xlsx')
"
Adjust file paths if they differ. Result types should include RankOrderResult, GridRatedResult, GridBinaryPivotResult.
Open the output workbook and visually confirm:

Q22, Q33, Q34, Q35, Q43 sheets exist with rank-order tables (counts per rank per option)
Q30 and Q36 show means per row × Winner/Other columns with delta
Q26 and Q41 show 2D pivot (stakeholder rows × role columns)


Spot-check three numbers against the inspo file:

Q30 Q30r11 Winner mean ≈ 8.62
Q26 Q26r11 first c-column count ≈ 23
Q43 Q43r12 rank-1 count ≈ 63



Paste back:
Tests: X passed / Y failed
Engine run: Results=X, Skips=Y, types=[...]
Visual: all sheets present / list missing
Spot-checks:
  - Q30 Q30r11 Winner mean: expected 8.62, got X
  - Q26 Q26r11 first c-col count: expected 23, got X
  - Q43 Q43r12 rank-1 count: expected 63, got X
If everything green and numbers match → classifier overhaul shipped.
If anything diverges → paste back, don't fix locally.