# Replit Prompt — Final classifier overhaul verification

Codex returned the last fix. One file changed: `src/excel_exporter.py` (function `_short_label_for_question` updated with divergence detection). All other files from the previous round stay as they are.

This is the final verification step. After this, the entire classifier overhaul is shipped or held based on what you see.

## Step 1 — Apply the file

Replace `src/excel_exporter.py` with the version from Codex's latest reply. No other files change.

## Step 2 — Confirm tests are green

```bash
python -m pytest tests/ -v 2>&1 | tail -30
```

Expect: **457 passing, 0 failing.** If anything fails, paste the failure messages — do not modify anything to make them pass.

## Step 3 — Run the engine on BCN sample data

```bash
python -c "
from pathlib import Path
from src.io import load_inputs
from src.datamap_parser import parse_datamap
from src.question_classifier import classify_questions
from src.single_cut.engine import compute_single_cuts
from src.calculation_log import CalculationLog
from src.excel_exporter import export_single_cuts

raw_path = Path('sample_data/BCN_LTB_raw_data.xlsx')   # adjust if filename differs
map_path = Path('sample_data/BCN_LTB_datamap.xlsx')    # adjust if filename differs

decoded_df, data_map, _report = load_inputs(raw_path, map_path)
schema = classify_questions(data_map, set(decoded_df.columns))
log = CalculationLog()
results, skips = compute_single_cuts(schema, decoded_df, log)
print(f'Results: {len(results)}, Skips: {len(skips)}')
print(f'Result types: {sorted(set(type(r).__name__ for r in results))}')

export_single_cuts(
    results=results,
    schema=schema,
    log=log,
    output_path=Path('/tmp/bcn_test_output.xlsx'),
)
print('Exported to /tmp/bcn_test_output.xlsx')
"
```

If file paths differ in your Replit environment, run `ls -la sample_data/` first and adjust. Paste any errors back.

**Key thing to look for in the print output:** the `Result types:` line should include `RankOrderResult`, `GridRatedResult`, and `GridBinaryPivotResult` — that confirms the new calculators actually fired on real data.

## Step 4 — Visual inspection

Download `/tmp/bcn_test_output.xlsx` and verify:

| Sheet | Expected layout | Status |
|---|---|---|
| Q22 | Rank table — counts per rank position per option | present / missing |
| Q33 | Same shape | present / missing |
| Q34 | Same shape | present / missing |
| Q35 | Same shape | present / missing |
| Q43 | Same shape | present / missing |
| Q30 | Mean per row × Winner/Other columns + Delta | correct format / wrong |
| Q36 | Same as Q30 | correct format / wrong |
| Q26 | 2D pivot — stakeholder rows × role columns | correct format / wrong |
| Q41 | Same as Q26 | correct format / wrong |

## Step 5 — Spot-check three exact numbers

Compare against the inspo file `260310_Likelihood_to_Buy_survey_cutter_v1.xlsx`:

| Cell | Expected | Got? |
|---|---|---|
| Q30 — Q30r11 (or 2H_Q30r11) row, Winner column mean | ≈ 8.62 | ? |
| Q26 — Q26r11 row, first c-column count | ≈ 23 | ? |
| Q43 — Q43r12 row, rank-1 count | ≈ 63 | ? |

## Paste back

Reply with:

```
Step 1 — File applied: yes
Step 2 — Test count: 457 passing / N passing M failing
Step 3 — Engine run: Results=X Skips=Y, types included: [...]
Step 4 — Visual inspection: 
   - Rank questions (Q22 Q33 Q34 Q35 Q43): all present / list missing
   - Q30 layout: correct (means + delta) / wrong (paste what you see)
   - Q36 layout: correct / wrong
   - Q26 layout: correct (2D pivot) / wrong
   - Q41 layout: correct / wrong
Step 5 — Spot-checks:
   - Q30 Q30r11 Winner mean: expected 8.62, got X
   - Q26 Q26r11 first c-col count: expected 23, got X
   - Q43 Q43r12 rank-1 count: expected 63, got X
```

## What this completes

If Step 2 shows 457 passing AND Step 5 numbers match within rounding:
- 5 question types previously missing or broken (Q22, Q33, Q34, Q35, Q43, Q30, Q36, Q26, Q41) now produce correct output
- The classifier overhaul is shipped
- Demo workbook is unblocked

If anything diverges, paste back exactly what you see and we'll diagnose.

## Constraints

- Don't touch `app.py` — UI work is separate
- Don't touch any other source files — this is a verification round, not a development round
- Don't run the Streamlit app yet — engine layer first
- Don't try to fix divergences locally — paste them back to me

We're at the finish line.