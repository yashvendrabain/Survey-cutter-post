ROLLBACK ROUND 5K.3 — Decoder regression on Q4-style SINGLE_SELECT.

CRITICAL EVIDENCE (verified in survey_analysis__46_.xlsx from real winvslag2024 run):

Q4 in _RawData column J: int values 5, 6, 4, 7, ... (raw codes, NOT decoded to labels)
Q1 in _RawData column B: str "Full-time" (decoded correctly)
Q2 in _RawData column C: str "Publicly held company" (decoded correctly)

Q1, Q2, Q4 are ALL SINGLE_SELECT questions with identical structure (single column + integer codes). The decoder option-maps Q1 and Q2 but skips Q4.

This is a REGRESSION introduced by Round 5K.3. Before 5K.3, Q4 was decoded correctly. After 5K.3, it's not.

The formulas in the workbook then do:
  =COUNTIFS(Q4_data, "2,000 to 4,999 employees", ...) 
But Q4_data contains integer 5, not the label. Every COUNTIFS returns 0 when Excel recalculates → green zeros across Q4 cross-cut.

PLUS: Q27, Q97, Q14 STILL show "Selected" in _RawData. 5K.3 didn't fix the original bug either.

Two bugs now:
1. Original (still broken): GRID columns collapse to "Selected"
2. New regression: SINGLE_SELECT Q4 not option-mapped

ACTION

Roll back raw_decoder.py to the BEFORE-5K.3 version. Check git for the previous version:

cd artifacts/survey-insight-engine && git log --oneline -- src/raw_decoder.py | head -5

If there's a pre-5K.3 commit, revert raw_decoder.py to it:

git show <prev_commit>:src/raw_decoder.py > /tmp/raw_decoder_prev.py
diff /tmp/raw_decoder_prev.py src/raw_decoder.py | head -50

Confirm the diff shows the Round 5K.3 additions. Then:

cp /tmp/raw_decoder_prev.py src/raw_decoder.py

Also revert the new test file:
git checkout HEAD~ -- tests/test_raw_decoder_grid_preservation_real.py 2>/dev/null || rm tests/test_raw_decoder_grid_preservation_real.py

Run the tests to confirm rollback restored prior state:

cd artifacts/survey-insight-engine && pytest tests/test_raw_decoder.py tests/test_question_classifier.py -v 2>&1 | tail -15

Then restart Streamlit.

ALTERNATIVELY: if git history isn't preserved, find the attached_assets/raw_decoder*.py from before 5K.3 (Codex uploaded the 5K.2 version at some point). Look for files like raw_decoder_178*.py with timestamps from BEFORE the 5K.3 install:

ls -lt attached_assets/raw_decoder*.py

The OLDEST raw_decoder file is most likely the pre-5K.3 baseline.

After rollback, the user will:
- Re-upload winvslag2024
- Re-verify Q4 decoder works (column J shows labels)
- Re-verify Q1, Q2 still work
- Accept that Q27/Q14 still show "Selected" (back to known state)

We'll plan a fresh round 5K.4 with REAL fixture testing, not synthetic.

REPORT BACK
- Whether git history allows clean revert OR we have to manually find prior version
- Test count after rollback
- Confirmation user can re-upload winvslag2024 and Q4 works again