Run Analysis silently failing again after Round 5K install. Same symptom as before the heartbeat fix — ~30s spinner then return to empty.

The Round 5J heartbeat (_run_with_status_heartbeat) was verified intact in STEP 5 of the install. Yet the symptom is back. Two hypotheses:
(a) The classify_questions phase (which now invokes the Round 5K new matchers) is throwing an exception that doesn't propagate via future.result()
(b) The new matchers have a pathological case on winvslag2024 that causes them to hang (infinite loop or O(n²) scan)
(c) The browser session is just stale and a hard refresh would fix it

DIAGNOSTIC

STEP 1. Eliminate hypothesis (c) — verify the user actually has a fresh session.

Tell me which browser tab Streamlit is running in. Is it the same tab from before the 5K install? If yes, instruct them to close ALL Streamlit tabs and open fresh. This is the fastest fix for stale state.

STEP 2. Run the pipeline headlessly to reproduce.

If user confirms they did hard-refresh in fresh tab and bug persists, run this in Replit shell:

cd artifacts/survey-insight-engine && timeout 180 python3 << 'EOF'
import time
import logging
logging.basicConfig(level=logging.INFO)

from src.io import load_survey_inputs
from src.question_classifier import classify_questions

class FakeUploadedFile:
    def __init__(self, path):
        from pathlib import Path
        self._path = Path(path)
        self.name = self._path.name
        self._bytes = self._path.read_bytes()
        self._pos = 0
    def read(self, size=-1):
        data = self._bytes[self._pos:] if size == -1 else self._bytes[self._pos:self._pos+size]
        self._pos = len(self._bytes) if size == -1 else self._pos + size
        return data
    def seek(self, pos, whence=0):
        if whence == 0: self._pos = pos
        elif whence == 1: self._pos += pos
        elif whence == 2: self._pos = len(self._bytes) + pos
    def getvalue(self): return self._bytes
    def getbuffer(self):
        import io
        return memoryview(self._bytes)

# Find winvslag2024
import glob
candidates = glob.glob("attached_assets/winvslag*.xlsx") + glob.glob("/tmp/winvslag*.xlsx") + glob.glob("sample_data/winvslag*.xlsx") + glob.glob("**/winvslag*.xlsx", recursive=True)
print(f"winvslag candidates: {candidates}")
if not candidates:
    print("CANNOT FIND winvslag2024 locally — testing with BCN_combined instead")
    candidates = glob.glob("sample_data/BCN_combined.xlsx") + glob.glob("attached_assets/BCN*.xlsx")
    print(f"BCN candidates: {candidates}")
if not candidates:
    print("NO TEST FILES FOUND — cannot reproduce")
    raise SystemExit()

t0 = time.time()
print(f"[{time.time()-t0:.1f}s] Loading {candidates[0]}...")
f = FakeUploadedFile(candidates[0])
load_result = load_survey_inputs([f])
print(f"[{time.time()-t0:.1f}s] Load done. Decoded df shape: {load_result.decoded_df.shape}")

t1 = time.time()
print(f"[{time.time()-t0:.1f}s] Starting classify_questions...")
schema = classify_questions(load_result.data_map, load_result.raw_df)
print(f"[{time.time()-t0:.1f}s] Classify done in {time.time()-t1:.1f}s. Questions: {len(schema.questions)}")
EOF

If this prints all stages including the classify_done line, the pipeline works headlessly and the issue IS the browser/session. Tell user to hard-refresh in incognito.

If this hangs (no output for >60s on the classify line), Round 5K introduced a slowdown. The new grid matchers likely have an O(n²) scan over raw columns.

If this raises an exception, paste the traceback. Round 5K has a real bug.

STEP 3. If headless reproduction succeeds without hang/error, the bug is browser-side. Restart Streamlit aggressively:

# Stop streamlit
pkill -f streamlit

# Wait
sleep 5

# Restart with explicit websocket settings
cd artifacts/survey-insight-engine && STREAMLIT_SERVER_WEBSOCKET_PING_INTERVAL=10 streamlit run app.py --server.websocketCompression=false 2>&1 | head -20 &
sleep 3

Confirm HTTP 200 then tell user to open Streamlit URL in incognito mode (NOT just hard-refresh — incognito completely bypasses cached state).

REPORT BACK

- STEP 1 status
- STEP 2 output (especially the classify timing line — if classify takes >10s, Round 5K is slowing things down)
- STEP 3 if needed