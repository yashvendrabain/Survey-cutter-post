Day 6 standby. Codex is finishing the single-cut engine today: 
grid calculator + orchestrator. Verification only.

PATH: cd artifacts/survey-insight-engine

When Codex delivers, I will paste the files. You will:

1. Save each file. Note: src/models.py is being MODIFIED 
   (one new SkipRecord dataclass added). Codex is pasting 
   only the addition with surrounding context — apply 
   carefully without disturbing other classes.

2. Run all 7 test suites in order:
     cd artifacts/survey-insight-engine
     python -m unittest tests.test_grid -v
     python -m unittest tests.test_single_cut_engine -v
     python -m unittest tests.test_calculation_log -v
     python -m unittest tests.test_calc_primitives -v
     python -m unittest tests.test_single_select -v
     python -m unittest tests.test_multi_select -v
     python -m unittest tests.test_numeric -v
   Paste each output in full.

3. Run the FULL ENGINE on the real file. This is the 
   end-to-end Day 6 milestone:

     cd artifacts/survey-insight-engine && python -c "
     from collections import Counter
     import time
     from src.datamap_parser import parse_datamap
     from src.raw_decoder import decode_raw_data
     from src.question_classifier import classify_questions
     from src.calculation_log import CalculationLog
     from src.single_cut import compute_single_cuts
     from src.models import QuestionType
     
     dm = parse_datamap('sample_data/Datamap_sample.xlsx')
     df, qr = decode_raw_data(
         'sample_data/Rawdata_sample.xlsx', dm)
     schema = classify_questions(
         dm, df.columns.tolist(),
         respondent_id_column='record',
         total_respondents=len(df),
         source_rawdata_path='sample_data/Rawdata_sample.xlsx'
     )
     log = CalculationLog()
     
     start = time.time()
     results, skips = compute_single_cuts(schema, df, log)
     elapsed = time.time() - start
     
     print(f'=== ENGINE RUN ===')
     print(f'Total questions in schema: '
           f'{len(schema.questions)}')
     print(f'Results produced:           {len(results)}')
     print(f'Skips:                      {len(skips)}')
     print(f'Audit log records:          {len(log)}')
     print(f'Elapsed:                    {elapsed:.2f} seconds')
     
     print()
     print('=== RESULTS BY TYPE ===')
     types = Counter(r.question_type.value for r in results)
     for t, n in sorted(types.items()):
         print(f'  {t}: {n}')
     
     print()
     print('=== SKIPS BY REASON ===')
     skip_reasons = Counter(s.skip_reason for s in skips)
     for r, n in sorted(skip_reasons.items()):
         print(f'  {r}: {n}')
     
     print()
     print('=== CALCULATION ERRORS ===')
     errors = [s for s in skips 
               if s.skip_reason == 'calculation_error']
     print(f'  count: {len(errors)}')
     for s in errors:
         print(f'  {s.canonical_id} '
               f'({s.question_type.value}): '
               f'{s.details}')
     
     print()
     print('=== SAMPLE RESULTS ===')
     for r in results[:5]:
         q = schema.get_question(r.question_id)
         print(f'  {r.question_id:25s} '
               f'{r.question_type.value:25s} '
               f'n={r.valid_n}/'
               f'{r.valid_n + r.missing_n}')
     
     print()
     print('=== AUDIT LOG SAMPLE ===')
     for rec in log.all_records()[:3]:
         print(f'  {rec.metric_name:20s} '
               f'q={rec.source_question_id:15s} '
               f'denom={rec.denominator}')
     "

   Paste the FULL output. The elapsed time is interesting — 
   we want to know if the engine runs in single-digit seconds 
   on 1100 respondents × ~75 eligible questions.

4. Do not modify any file Codex writes.