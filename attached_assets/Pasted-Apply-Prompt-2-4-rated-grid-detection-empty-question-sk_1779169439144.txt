Apply Prompt 2.4 (rated-grid detection + empty-question skip) to the Survey Analysis Engine.
Files to replace from Codex:

src/question_classifier.py — _is_numeric_label now parses leading numeric token from labels like "10 (extremely high)". _numeric_label_value extracts the number. Rated grids with text-suffixed scale endpoints now classify as GRID_RATED instead of GRID_CATEGORICAL.
src/single_cut/engine.py — new _all_raw_columns_empty check skips questions with all-null raw columns before dispatch.
tests/test_question_classifier.py + tests/test_single_cut_engine.py + tests/test_excel_exporter.py — new regressions.

What this iteration ships:

Q30/Q36 in BCN will classify as GRID_RATED → dual-block renderer (left: response count/%, right: Winner/Other/Delta × Mean/Median).
Q26/Q41 in BCN (all columns empty) will be skipped — no blocks rendered, listed in skips/warnings.

Known recurring Codex regressions — check and fix BEFORE running tests:

parents[1] / "artifacts" / "survey-insight-engine" / "app.py" → parents[1] / "app.py"
Stale AI-label assertions ("...Export Question" → "...Export")
LONG_ID exact match → startswith prefix
_grid_base_sub_column_id regex: Codex shipped r"c\d+$" again (loose version). Tighten to require r\d+ before c\d+ — same patch you've applied in the last 5 iterations. Without this, columns like Q15c1 (no r prefix) get incorrectly stripped.
_grid_row_labels_for_spec fallback: ensure the fallback when stripped base isn't in lookup still exists (same patch from Prompt 2.1).

Run after replacing + patching:

pytest full suite. Codex reported 412. Confirm exact count.
Generate small sample workbook → outputs/survey_prompt2_4_sample.xlsx. Verify clean generation.
Generate BCN workbook → outputs/survey_prompt2_4_bcn.xlsx.

CRITICAL BCN verification:

Report heading-count dict:
Q26: <N> heading(s) — expected: 0 (empty, skipped)
Q30: <N> heading(s) — expected: 1 (merged rated grid)
Q36: <N> heading(s) — expected: 1 (merged rated grid)
Q38: <N> heading(s) — expected: >1 (binary select, unmerged)
Q41: <N> heading(s) — expected: 0 (empty, skipped)

Q30 layout check: does the block show the dual-block format?

Left table: # of respondents | % of respondents per criterion
Right table: Winner - All | Other considered vendor | Delta each with Mean | Median sub-columns
If it still shows the GRID_CATEGORICAL layout (scale values 8/7/9/6 as columns with Count/%), the GRID_RATED detection or possible_role propagation is still broken. Report this — do not patch.


Q36 layout check: same as Q30.
Q26/Q41 absent from output: confirm zero heading rows for both. Check the Single_Cut_Index or Warnings sheet for skip entries mentioning "all raw columns empty in dataset".
Regression: Q38 untouched: still renders as binary-select blocks.
Regression: cross-tab + filters still work on any non-grid block.

Report all 6 items. If Q30/Q36 layout is wrong, paste the first 5 data rows of the block so I can see what renderer was used.