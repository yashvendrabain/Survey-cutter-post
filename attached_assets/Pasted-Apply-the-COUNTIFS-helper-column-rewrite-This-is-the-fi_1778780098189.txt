Apply the COUNTIFS helper-column rewrite. This is the final fix for 
the Excel 365 implicit-intersection issue.

Codex delivered:
- src/excel_exporter.py — full rewrite using COUNTIFS + helper columns
- _RawData now has per-filter _match columns + passes_workbook_filters column
- Theme sheet count formulas are now: 
  =COUNTIFS(Q17_data, "Revenue to grow 3-5%", passes_workbook_filters_data, 1)
- Numeric formulas: AVERAGEIFS, MINIFS, MAXIFS
- 354 tests pass
- No SUMPRODUCT, no SEARCH, no INDIRECT, no IF, no @ risk

STEP 1 — REPLACE src/excel_exporter.py verbatim from Codex attachment.

STEP 2 — RUN TESTS
    pytest --tb=short -q
Expected: 354+ pass

STEP 3 — GENERATE FRESH WORKBOOK with autocalc + verify with LibreOffice

cd artifacts/survey-insight-engine && python3 << 'PYEOF'
import sys, os; sys.path.insert(0, ".")
from src.io import load_raw_data, load_datamap
from src.datamap_parser import parse_datamap
from src.question_classifier import classify_questions
from src.raw_decoder import decode_raw_data
from src.single_cut_engine import compute_single_cuts
from src.excel_exporter import export_single_cuts
from src.ai_insights import (
    categorize_questions_into_themes,
    categorize_demographic_questions,
    generate_short_labels,
)
import openpyxl, subprocess

datamap = load_datamap("sample_data/Datamap_sample.xlsx")
schema = classify_questions(parse_datamap(datamap))
decoded_df = decode_raw_data(load_raw_data("sample_data/Rawdata_sample.xlsx"), schema)
results, skips, log, qr = compute_single_cuts(decoded_df, schema)

qfs = [{"question_id": q.canonical_id, "question_text": q.question_text,
        "question_type": q.question_type.value, "is_demographic": q.is_demographic}
       for q in schema.questions]
themes = categorize_questions_into_themes(qfs, cache={})

dqs = [{"question_id": q.canonical_id, "question_text": q.question_text,
        "question_type": q.question_type.value}
       for q in schema.demographic_questions()]
demo_priority = categorize_demographic_questions(dqs, cache={})

short_labels = generate_short_labels(
    [{"question_id": q.canonical_id, "question_text": q.question_text}
     for q in schema.questions], cache={})

os.makedirs("outputs", exist_ok=True)
out = "outputs/survey_v6_countifs.xlsx"
export_single_cuts(
    results=results, skips=skips, schema=schema,
    quality_report=qr, log=log, output_path=out,
    themes=themes, decoded_df=decoded_df,
    demo_priority=demo_priority, short_labels=short_labels,
)

wb = openpyxl.load_workbook(out)
wb.calculation.calcMode = "auto"
wb.calculation.fullCalcOnLoad = True
wb.calculation.forceFullCalc = True
wb.save(out)

# Inspect a count formula
rg = wb["Revenue & Growth"]
for r in range(1, rg.max_row + 1):
    a = rg.cell(row=r, column=1).value
    if isinstance(a, str) and 'Revenue to grow 3-5%' in a:
        formula = rg.cell(row=r, column=2).value
        print(f"=== B{r} formula ===")
        print(formula)
        print()
        print(f"Starts with =COUNTIFS: {formula.startswith('=COUNTIFS')}")
        print(f"Contains SUMPRODUCT: {'SUMPRODUCT' in formula}")
        print(f"Contains SEARCH: {'SEARCH' in formula}")
        print(f"Contains @: {'@' in formula}")
        break

# Verify _RawData has helper columns
rd = wb['_RawData']
headers = [c.value for c in list(rd.iter_rows(min_row=1, max_row=1))[0]]
match_cols = [h for h in headers if h and '_match' in str(h)]
print(f"\n_match columns in _RawData: {len(match_cols)}")
print(f"First 5: {match_cols[:5]}")
print(f"passes_workbook_filters column present: {'passes_workbook_filters' in headers}")

# LO recalc
subprocess.run(["libreoffice","--headless","--calc","--convert-to","xlsx",
                "--outdir","outputs/recalc_v6", out],
               capture_output=True, text=True, timeout=300)

wb_c = openpyxl.load_workbook("outputs/recalc_v6/survey_v6_countifs.xlsx", data_only=True)
rg_c = wb_c["Revenue & Growth"]
print("\n=== Q17 unfiltered distribution AFTER LO recalc ===")
in_section = False
total = 0
for r in range(1, rg_c.max_row + 1):
    a = rg_c.cell(row=r, column=1).value
    if isinstance(a, str) and 'Q17' in a and 'Planned' in a:
        in_section = True
        continue
    if in_section and a == 'Option':
        continue
    if in_section:
        b = rg_c.cell(row=r, column=2).value
        d = rg_c.cell(row=r, column=4).value
        if isinstance(b, (int, float)):
            total += b
            print(f"  {str(a)[:40]:42s} count={b!r:8s} denom={d!r}")
        if d == 1100 and total >= 1100:
            break

print(f"\nTotal Q17 sum: {total}")
print(f"Expected: 1100 (no filter)")
PYEOF

EXPECTED OUTPUT:
- Formula starts with =COUNTIFS (yes)
- Contains SUMPRODUCT (no)
- Contains SEARCH (no — should be empty)
- _match columns: 8+ (one per demographic filter)
- passes_workbook_filters present: yes
- LO recalc Q17 total: 1100

If all match, PRESENT THE WORKBOOK to user via direct download.

If user opens it in Excel 365 and counts still wrong, that means 
COUNTIFS itself somehow breaks — which would be unprecedented. 
COUNTIFS has been stable in Excel for 17 years.

Report:
- Test count
- Formula inspection results
- LO Q17 sum
- File path