Apply v11 to the Survey Analysis Engine.
Files to replace from Codex:

src/excel_exporter.py — full replacement
tests/test_excel_exporter.py — updated tests

What this iteration ships (4 coordinated fixes):

All_Questions dropdown source now contains only eligible single-select question labels (Q-prefix + AI short label). Country names, version flags, and term flags no longer leak into the per-question filter / cross-tab dropdowns.
Numeric allocation tables (Q20, Q21, Q33) now render as Option | Mean | Median | Denominator — one row per option, column-oriented. Min, Max, Std Dev columns removed.
Question heading rows on theme sheets now show "Q7 - Org Size" format (Q-number + AI short label). Full original question text attached as Excel hover comment.
Bain red bar on heading row spans columns A through N (full table width), not just column A.

Run after replacing:

pytest (full suite) — Codex reported 381 passes. Confirm.
Verify Portkey is reachable: python -c "import os; print('Portkey key set:', bool(os.environ.get('PORTKEY_API_KEY')))". If False, set it in Replit Secrets.
Generate a workbook against sample_data/Rawdata_sample.xlsx + sample_data/Datamap_sample.xlsx → outputs/survey_v11_sample.xlsx.
Generate a workbook against BCN file → outputs/survey_v11_bcn.xlsx.

Verification — confirm all checks:

Dropdown fix: On any theme sheet, click the "Filter Q" dropdown on a per-question filter row. The list should show only question labels like "Q1 - Country", "Q4 - Company Size", "Q7 - Seniority". NO country names ("India", "Spain"), NO industry names ("Dairy"), NO version flags ("Version 2").
Cross-tab dropdown: Same check on the "Cross-tab by" dropdown — same clean list.
Numeric table layout: Find Q20 (revenue allocation) on a theme sheet. Confirm format is Option | Mean | Median | Denominator with one row per allocation option. No "Min" / "Max" / "Std" rows.
Heading format: Theme sheet question headings show "Q7 - Seniority" style (short).
Hover comment: Hovering over the heading cell in Excel reveals the full original question text.
Red bar spans width: Heading row red fill extends across all 4 columns (A-D), not just A.
Regression — small sample filter still propagates: On the sample workbook (under 2,000 rows), set a Country filter and confirm Q17 counts change.

Report: test count, all 7 verification checks (pass/fail), and one screenshot of the per-question filter dropdown on a BCN theme sheet (the most important verification — that's the bug this fixes).