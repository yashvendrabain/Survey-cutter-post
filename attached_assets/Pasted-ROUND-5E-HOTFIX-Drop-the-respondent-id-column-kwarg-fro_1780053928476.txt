ROUND 5E HOTFIX — Drop the respondent_id_column kwarg from _build_segment_masks calls in excel_exporter.py.

PROBLEM

Round 5E's excel_exporter.py calls _build_segment_masks(...) with respondent_id_column=schema.respondent_id_column at 3 sites (lines 397, 411, 435). The production _build_segment_masks signature does NOT accept this parameter. Codex pre-emptively added the kwarg expecting Round 5G to extend the signature — but 5G hasn't shipped.

The kwarg is unnecessary for Round 5E's scope (categorical / numeric_threshold / quartile modes don't use respondent IDs). Remove the kwarg from all 3 call sites.

STEP 1: Find all call sites.

cd artifacts/survey-insight-engine && grep -n "_build_segment_masks(" src/excel_exporter.py

Expected: 3 matches around lines 397, 411, 435.

STEP 2: Show the context of each call.

sed -n '395,415p' src/excel_exporter.py
sed -n '425,445p' src/excel_exporter.py

Paste both outputs so I can see the exact line where respondent_id_column= appears.

STEP 3: Apply the fix.

python3 << 'EOF'
import re
path = "src/excel_exporter.py"
with open(path) as f:
    content = f.read()

# Remove "respondent_id_column=schema.respondent_id_column," lines (with trailing comma and indentation)
pattern = re.compile(r'\n\s+respondent_id_column=schema\.respondent_id_column,?')
new_content, n = pattern.subn('', content)

if n == 0:
    print("PATTERN NOT FOUND — pasting first 30 chars of each call site:")
    for m in re.finditer(r'_build_segment_masks\([^)]*\)', content):
        print(f"  pos {m.start()}: {m.group()[:120]}")
else:
    with open(path, "w") as f:
        f.write(new_content)
    print(f"Removed {n} occurrences of respondent_id_column= kwarg.")
EOF

STEP 4: Verify no more occurrences remain.

grep -n "respondent_id_column" src/excel_exporter.py

Expected: zero matches (the kwarg is gone) OR matches that are unrelated to _build_segment_masks calls (e.g., reading schema.respondent_id_column elsewhere for other purposes — those are fine).

STEP 5: Re-run the WVL tests.

pytest tests/test_excel_exporter_winners_vs_laggards.py -v 2>&1 | tail -35

Expected: 30 tests pass.

If they still fail with a different TypeError or missing parameter, paste the new traceback. There may be a second pre-emptive Round 5G dependency we need to strip.

STEP 6: Full suite chunked.

Expected: ~592 total.

STEP 7: Continue from the original Round 5E integration prompt at STEP 4 (restart Streamlit, regenerate WVL workbook, end-to-end verification).

REPORT BACK

- STEP 1 grep count (must be 3)
- STEP 2 context output
- STEP 3 fix result (must remove all 3)
- STEP 4 verification (no respondent_id_column in _build_segment_masks calls)
- STEP 5 WVL test result
- STEP 6 full suite total
- Then continue with STEPS 4-6 from original Round 5E integration prompt