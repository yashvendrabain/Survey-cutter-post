Apply Prompt 2.5 (final classifier fixes) to the Survey Analysis Engine and verify against BCN.
File to replace from Codex:

src/question_classifier.py — sibling merge no longer blanket-skips binary role; binary-encoded siblings WITH c-column structure now merge and reclassify as GRID_CATEGORICAL. Rated detection now uses label-derived numeric scale instead of value_range gate, so Q30/Q36 with value_range=(1,12) but labels spanning 0-10 classify as GRID_RATED.
tests/test_question_classifier.py — added regressions.

What this iteration ships:

Q26 and Q41 in BCN: merge into one GRID_CATEGORICAL block each (the binary-skip blocker is bypassed via c-column detection).
Q30 and Q36 in BCN: classify as GRID_RATED → dispatch to dual-block renderer.
Q38 in BCN: unchanged (no sibling merge candidates — single parent question).

Heads-up — Codex actually shipped the tightened _grid_base_sub_column_id regex this time (r"^(.+r\d+)c\d+$"). Verify before patching. Other recurring regressions still to check:

parents[1] / "artifacts" / "survey-insight-engine" / "app.py" in tests
Stale AI-label assertions
LONG_ID startswith match

Run after replacing + patching:

pytest full suite. Codex reported 417 passes. Confirm exact count.
Generate small sample workbook → outputs/survey_prompt2_5_sample.xlsx. Confirm clean generation.
Generate BCN workbook → outputs/survey_prompt2_5_bcn.xlsx via standard streaming workflow.

CRITICAL BCN verification — this is the make-or-break iteration:

Report heading-count dict:
Q26: <N> heading(s) — expected: 1 (categorical, merged)
Q30: <N> heading(s) — expected: 1 (rated, merged)
Q36: <N> heading(s) — expected: 1 (rated, merged)
Q38: <N> heading(s) — expected: >1 (binary, unmerged — single parent)
Q41: <N> heading(s) — expected: 1 (categorical, merged)

Q30 layout: confirm dual-block format:

Left table: # of respondents | % of respondents per criterion
Right table: Winner - All | Other considered vendor | Delta × Mean | Median
If still GRID_CATEGORICAL layout (scale-value columns), paste the first 5 data rows of the block and STOP — report the failure.


Q36 layout: same as Q30.
Q26 layout: confirm one merged block, categorical format:

Rows = stakeholder groups (IT, Security, Risk, Compliance, Finance...)
Columns = role categories (Blocked Vendors, Scored Vendors, Recommended, Restricted...)
Count + % side-by-side per role category
Total respondents + Total responses footers


Q41 layout: same as Q26.
Q38 regression: still renders as multiple binary blocks. Cross-tab still produces 2D table.

Report all 6 items. Include 3 screenshots: Q30 (rated dual-block), Q26 (categorical), and any one cross-tab (regression check).
If everything passes, we ship the engine and move to Prompt 3 (UX features for app.py). This is the iteration we've been working toward for 6 cycles.