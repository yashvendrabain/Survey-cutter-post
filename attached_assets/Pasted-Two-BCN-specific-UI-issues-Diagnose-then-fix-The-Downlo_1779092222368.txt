Two BCN-specific UI issues. Diagnose then fix.

The "Download single-cut workbook" button is disabled (greyed out, red-cross cursor on hover) after running analysis on BCN. The same button works fine on the small sample file.
The in-app filter selector doesn't update tables when changed on BCN. Works fine on small sample.

Step 1 — Find the disabled condition.
bashgrep -n "Download single-cut\|download_button\|single.cut.workbook" app.py
Show me the button code with surrounding context, especially its disabled= parameter.
Step 2 — Add temporary debug output. Just before that button, insert:
pythonst.write({
    "run_complete": st.session_state.get("run_complete"),
    "results_count": len(st.session_state.get("results", []) or []),
    "skips_count": len(st.session_state.get("skips", []) or []),
    "active_df_shape": getattr(st.session_state.get("active_df"), "shape", None),
    "decoded_df_shape": getattr(st.session_state.get("decoded_df"), "shape", None),
    "schema_present": st.session_state.get("schema") is not None,
    "global_filter_state": st.session_state.get("global_filter_state"),
})
Reload app, upload BCN, click Run Analysis, wait for completion, screenshot what st.write shows above the disabled button.
Step 3 — Diagnose filter selector. With BCN loaded and Run Analysis complete, change a value in the in-app filter selector (e.g., set Country to a specific value). Then:

Does the page rerun (spinner appears)?
After rerun, what does st.session_state.get("global_filter_state") contain?
Do the single-cut tables show updated counts, or unchanged counts?
Any tracebacks in tail -150 /tmp/logs/artifactssurvey-insight-engine_web_*.log?

Step 4 — Once Step 2 reveals which condition is wrongly True for BCN, fix it surgically. The fix is likely one line — either a count check that should be >= 0 instead of > 0, a threshold check that bypasses BCN-size results, or a session state key that's not being set for large files.
Step 5 — Once Step 3 reveals whether the filter is failing to update state or failing to re-render, fix it. If state update fails, the widget callback is broken. If state updates but tables don't re-render, the recompute trigger is broken.
Report:

Output of st.write diagnostics with BCN loaded.
Output of st.write diagnostics after filter change attempted.
Tracebacks (if any) from log tail.
The disabled condition you found.
The fix you applied for each issue.
Test count after fixes (should still be 388+).

Constraint: the small sample workflow must continue to work end-to-end. Test both BCN and small sample after fixes.