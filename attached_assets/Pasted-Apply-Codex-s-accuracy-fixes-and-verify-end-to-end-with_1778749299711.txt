Apply Codex's accuracy fixes and verify end-to-end with LibreOffice 
recalc. This closes the critical accuracy bug that has blocked the 
workbook feature.

STEP 1 — REPLACE FILES VERBATIM

Replace these from Codex attachments:
- src/raw_decoder.py
- src/excel_exporter.py
- tests/test_raw_decoder.py (if attached)
- tests/test_excel_exporter.py (if attached)

Watch for the recurring path bug in test files:
    parents[1]/'artifacts'/'survey-insight-engine'/'app.py'
should be:
    parents[1]/'app.py'

STEP 2 — RUN TESTS

pytest --tb=short -q

Expected: 352+ tests pass. Paste any failures.

STEP 3 — END-TO-END RECALC VERIFICATION

This is the critical step Codex couldn't run. You have LibreOffice 
from the previous diagnostic round.

cd artifacts/survey-insight-engine
python3 << 'PYEOF'
import sys, os, subprocess
sys.path.insert(0, ".")
import openpyxl

from src.io import load_raw_data, load_datamap
from src.datamap_parser import parse_datamap
from src.question_classifier import classify_questions
from src.raw_decoder import decode_raw_data
from src.single_cut_engine import compute_single_cuts
from src.excel_exporter import export_single_cuts
from src.ai_insights import (
    categorize_questions_into_themes,
    categorize_demographic_questions,
    generate_short_labels,
)

datamap = load_datamap("sample_data/Datamap_sample.xlsx")
schema = classify_questions(parse_datamap(datamap))
decoded_df = decode_raw_data(load_raw_data("sample_data/Rawdata_sample.xlsx"), schema)

# Bug 1 sanity check
print("=" * 70)
print("BUG 1 — Decoder returns labels")
print("=" * 70)
for col in ["Q4", "Q13", "Q14", "QCountryGroups", "QIndustry"]:
    if col in decoded_df.columns:
        sample = decoded_df[col].dropna().unique()[:3].tolist()
        types = [type(v).__name__ for v in sample]
        print(f"  {col}: dtype={decoded_df[col].dtype}, sample={sample}, types={types}")

results, skips, log, qr = compute_single_cuts(decoded_df, schema)

themes = categorize_questions_into_themes(
    [{"question_id": q.canonical_id, "question_text": q.question_text,
      "question_type": q.question_type.value, "is_demographic": q.is_demographic}
     for q in schema.questions], cache={})
demo_priority = categorize_demographic_questions(
    [{"question_id": q.canonical_id, "question_text": q.question_text,
      "question_type": q.question_type.value}
     for q in schema.demographic_questions()], cache={})
short_labels = generate_short_labels(
    [{"question_id": q.canonical_id, "question_text": q.question_text}
     for q in schema.questions], cache={})

os.makedirs("outputs", exist_ok=True)
out = "outputs/post_fix.xlsx"
export_single_cuts(results=results, skips=skips, schema=schema,
    quality_report=qr, log=log, output_path=out,
    themes=themes, decoded_df=decoded_df,
    demo_priority=demo_priority, short_labels=short_labels)

# Bug 2 verification — inject Q14 filter (comma-bearing label) and recalc
wb = openpyxl.load_workbook(out, data_only=False)
fs = wb["Filters"]
q14_row = None
for r in range(1, 30):
    v = fs.cell(row=r, column=1).value
    if isinstance(v, str) and "Q14" in v:
        q14_row = r
        break

print(f"\n=" * 35)
print("BUG 2 — Filter with comma-bearing label")
print("=" * 70)
print(f"Q14 row on Filters: {q14_row}")

fs.cell(row=q14_row, column=2).value = "1,000 to 1,999 employees"
filtered_path = "outputs/post_fix_filtered.xlsx"
wb.save(filtered_path)

print("Recalculating with LibreOffice...")
subprocess.run(
    ["libreoffice", "--headless", "--calc", "--convert-to", "xlsx",
     "--outdir", "outputs/recalc_post", filtered_path],
    capture_output=True, text=True, timeout=300,
)

wb_c = openpyxl.load_workbook(
    f"outputs/recalc_post/{os.path.basename(filtered_path)}", 
    data_only=True,
)

# Find Q4 section
cs = None
for s in wb_c.worksheets:
    if s.title in ('_RawData','_Options','Filters','Run_Summary',
                   'Question_Metadata','Single_Cut_Index',
                   'Calculation_Log','Filter_Log','Warnings'):
        continue
    for r in range(1, s.max_row + 1):
        v = s.cell(row=r, column=1).value
        if isinstance(v, str) and "Software, Hardware" in v:
            cs = s
            break
    if cs:
        break

print(f"\nQ4 results on sheet '{cs.title}' with Q14 filter:")
print(f"{'Option':55s} {'Excel':>8s} {'Python':>8s} {'Match':>8s}")
print("-" * 85)

# Python truth
filtered_decoded = decoded_df[decoded_df['Q14'] == "1,000 to 1,999 employees"]
python_n = len(filtered_decoded)
print(f"Filtered sample size: Python={python_n}")
print()

all_match = True
for r in range(1, cs.max_row + 1):
    a = cs.cell(row=r, column=1).value
    b = cs.cell(row=r, column=2).value
    if not isinstance(a, str):
        continue
    if a in decoded_df['Q4'].dropna().unique():
        py_count = (filtered_decoded['Q4'] == a).sum()
        excel_count = b if isinstance(b, (int, float)) else 0
        match = "✓" if excel_count == py_count else "✗"
        if match == "✗":
            all_match = False
        print(f"  {str(a)[:53]:55s} {excel_count!r:>8s} {py_count!r:>8s} {match:>8s}")

print()
print("=" * 70)
print(f"ALL MATCH: {all_match}")
print("=" * 70)

PYEOF

PASTE THE FULL OUTPUT.

EXPECTED OUTCOME:
- Bug 1: decoded_df shows string labels like 'Finance', 'Software, 
  Hardware, Networking & IT Services' (not int codes)
- Bug 2: Excel counts match Python counts for every Q4 option 
  under Q14 filter
- "ALL MATCH: True"

If ALL MATCH is True, both bugs are fixed and the workbook feature 
is finally shipping correctly.

If ALL MATCH is False, paste the mismatched rows and we'll diagnose 
the remaining issue.

After verification passes, copy the workbook to where the user can 
download it:
    cp outputs/post_fix_filtered.xlsx ~/Downloads/

OR present it directly in chat via the standard download mechanism.