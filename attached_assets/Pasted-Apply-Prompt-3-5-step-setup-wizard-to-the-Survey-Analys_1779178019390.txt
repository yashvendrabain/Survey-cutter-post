Apply Prompt 3 (5-step setup wizard) to the Survey Analysis Engine.
Files to replace from Codex:

app.py — adds 5-step wizard between Upload and Run Analysis, with progress stepper, persistent session state under wiz_* keys, custom CSS for consultant-style flat design.
src/ui/wizard.py — new helpers module (pure functions: category assignments, theme conversion, schema overrides, distinct-value preview).
tests/test_ui_wizard.py — 5 wizard helper tests.

What this iteration ships:

Step 1: Categories editor (kanban-style columns, rename categories, move questions via "Move to" dropdown per card, remove with confirmation). Note: not drag-and-drop — Codex couldn't verify streamlit-sortables works in the workspace, fell back to dropdown-per-card move. Functionally equivalent, less flashy.
Step 2: Demographics review with checkboxes + ability to add non-demographic questions as filters.
Step 3: Number input for custom workbook filter count (0-5, default 2).
Step 4: Number input for per-question filter count (0-3, default 1).
Step 5: Radio for cross-cut consolidation (one sheet vs separate sheets).

Critical scope note — only Steps 1 and 2 actually affect the output workbook. Steps 3, 4, 5 store user choices in session state but the exporter doesn't yet have public parameters for custom filter count, per-question filter count, or cross-cut consolidation. That's intentional for this iteration — those wirings come in Prompt 3.1 after the wizard structure is verified. Until then, the exporter uses its default values for those settings regardless of what the user chose.
Heads-up on recurring Codex regressions — pre-empt:

parents[1] / "artifacts" / "survey-insight-engine" / "app.py" → parents[1] / "app.py"
Stale AI-label assertions
LONG_ID startswith match

Install dependency: Codex couldn't verify streamlit-sortables is available. Try installing it:
bashpip install streamlit-sortables
If it installs cleanly and works in Replit's environment, that's a future polish for drag-and-drop. For this iteration, keep the dropdown fallback Codex shipped.
Run after replacing:

pytest full suite. Codex reported 422 (note: Codex ran via unittest discover, not pytest, so the 423 number from previous iteration may differ). Confirm exact count.
Launch the Streamlit app and walk through the full flow:

Upload the small sample files (raw data + data map)
Wait for the prep pass (parse + classify + theme + demographics)
Verify the 5-step wizard appears
Step 1: confirm category columns render, "Move to" dropdowns work, rename via text input works, Remove button triggers confirmation
Step 2: checkboxes for each auto-detected demographic, "+ Add question as filter" dropdown adds new ones
Step 3: number input 0-5, default 2
Step 4: number input 0-3, default 1
Step 5: radio choice
Click "Run Analysis →" at the end of Step 5
Verify workbook generates and downloads


Open the generated workbook in Excel:

Confirm category assignments from Step 1 are honored (a question moved from Demographics → Pricing should appear under Pricing in the workbook)
Confirm demographics from Step 2 are honored (an unchecked demographic should NOT appear as a filter on the Filters sheet)
Note: Steps 3/4/5 won't visibly change the output yet — that's expected.



Report:

Test count
Pass/fail on each wizard step's interaction
Confirmation that workbook generation succeeds end-to-end through the wizard
1 screenshot of Step 1 (the most visual step) — this is what we'll iterate on for polish if needed
Any architectural snags