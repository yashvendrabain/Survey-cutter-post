ROUND 4 INTEGRATION — Install Codex's raw data decoder fix for numeric-typed questions (Q30, etc.) where labeled values like '10 (extremely high)' were being silently dropped from averages.

CONTEXT

Round 3 fixed exporter formulas (AVERAGEIFS, COUNTIFS) for grid_rated and rank_order questions. But the underlying _RawData sheet was still missing values: cells like '10 (extremely high)' were being left as strings, and the AVERAGEIFS ">=0" filter dropped them. Result: Q30r1 winner mean was 7.29 instead of the correct 8.22 (a 13% understatement caused by dropping the most enthusiastic responses).

Codex completed Round 4. The decoder now reads the data map's option labels for numeric questions and decodes '10 (extremely high)' → 10.0, treats 'This was not something I considered' as known N/A, and warns on unrecognized values.

I am uploading 8 modified/new files. Your job: install them, run the test suite, restart Streamlit, rerun the BCN analysis, and verify Q30 numbers are now correct.

FILES BEING UPLOADED

1. src/models.py — replaces existing. Adds three new optional fields to QuestionSpec: label_to_numeric_value (dict), na_label_set (frozenset), allowed_numeric_range (tuple or None).
2. src/datamap_parser.py — replaces existing. Adds derive_numeric_label_metadata() and attaches the three new fields to each QuestionSpec.
3. src/question_classifier.py — replaces existing. Minor changes to support numeric coercion routing.
4. src/raw_decoder.py — replaces existing. Adds decode_numeric_cell() and applies it per-cell to numeric-typed question columns before writing to _RawData. Note: implementation is a per-cell Python loop, not vectorized. This may add 10-30s to the BCN run — acceptable for this round, will optimize later if needed.
5. src/excel_exporter.py — replaces existing. Extends the Warnings sheet with a "Data Quality — Decoder Warnings" section that shows summary counts plus detail rows.
6. tests/test_raw_decoder_label_coercion.py — new test file.
7. tests/test_datamap_parser_numeric_coercion_setup.py — new test file.
8. tests/test_excel_exporter_decoder_warnings.py — new test file.

INSTRUCTIONS

STEP 1. Replace files
After upload, confirm each file is in the right path. Source modules go under src/, tests under tests/.

STEP 2. Run the test suite
Run:

cd artifacts/survey-insight-engine && pytest tests/ -v 2>&1 | tail -15

Expected: ~508 tests pass (494 from Round 3 + ~14 new). If anything fails, stop and paste the failure output before changing anything.

STEP 3. Restart Streamlit
Stop the current Streamlit process. Start it fresh:

cd artifacts/survey-insight-engine && streamlit run app.py

If startup fails with an ImportError or AttributeError, paste the traceback before changing anything.

STEP 4. Run the BCN analysis end-to-end and TIME IT
In the browser:
- Hard-refresh (Ctrl+Shift+R)
- Upload BCN raw data + data map (or combined file if that's the flow used)
- Click through the wizard
- Note the wall-clock time when you click "Run Analysis"
- Wait for the "Building Excel workbook..." status to complete
- Note the wall-clock time at completion
- Download the workbook

Report the elapsed time in your reply. We expect it to be 10-30s slower than Round 3 due to the per-cell decoder loop. If it takes longer than 90 seconds total, flag that — we'll need to vectorize the decoder.

STEP 5. Verify Q30 mean is now correct
In the Replit shell (NOT in the Streamlit process tab — a separate shell tab):

cd artifacts/survey-insight-engine && python3 << 'EOF'
import openpyxl, glob, os
files = sorted(glob.glob('outputs/survey_analysis*.xlsx'), key=os.path.getmtime, reverse=True)
if not files:
    print("NO OUTPUT FILE — STEP 4 didn't produce a downloadable workbook")
else:
    path = files[0]
    print(f"Checking: {path}\n")
    # data_only=True reads cached formula values (what Excel last computed)
    wb = openpyxl.load_workbook(path, data_only=True)
    ws = wb['Vendor Selection Process']
    found = False
    for r in range(280, 330):
        a = ws.cell(row=r, column=1).value
        if a == 'Q30r1':
            winner = ws.cell(row=r, column=3).value
            other = ws.cell(row=r, column=4).value
            delta = ws.cell(row=r, column=5).value
            print(f"Q30r1 (row {r}):")
            print(f"  Winner mean: {winner}")
            print(f"  Other vendor mean: {other}")
            print(f"  Delta: {delta}")
            print()
            if winner is not None:
                if 8.0 <= winner <= 8.5:
                    print("VERDICT: Decoder is working. Mean is in expected range (~8.22).")
                elif 7.0 <= winner <= 7.5:
                    print("VERDICT: Decoder NOT applied. Mean is still ~7.29 — labeled values being dropped.")
                else:
                    print(f"VERDICT: Unexpected mean {winner}. Investigate.")
            found = True
            break
    if not found:
        print("Q30r1 row not found in expected range. Searching all sheets...")
        for sn in wb.sheetnames:
            ws2 = wb[sn]
            for r in range(1, ws2.max_row + 1):
                if ws2.cell(row=r, column=1).value == 'Q30r1':
                    print(f"  Found Q30r1 on sheet '{sn}' row {r}, C value: {ws2.cell(row=r, column=3).value}")
EOF

Paste the full output. Pay special attention to the VERDICT line.

STEP 6. Verify decoder warnings appear
Same Replit shell:

cd artifacts/survey-insight-engine && python3 << 'EOF'
import openpyxl, glob, os
files = sorted(glob.glob('outputs/survey_analysis*.xlsx'), key=os.path.getmtime, reverse=True)
path = files[0]
wb = openpyxl.load_workbook(path, data_only=True)
if "Warnings" not in wb.sheetnames:
    print("No Warnings sheet found")
else:
    ws = wb["Warnings"]
    print(f"Warnings sheet has {ws.max_row} rows")
    print("\nFirst 30 non-empty rows:")
    shown = 0
    for r in range(1, ws.max_row + 1):
        vals = [ws.cell(row=r, column=c).value for c in range(1, 7)]
        if any(v is not None and v != '' for v in vals):
            print(f"  row {r}: {vals}")
            shown += 1
            if shown >= 30:
                break
EOF

Paste the output. We expect to see a section labeled "Data Quality — Decoder Warnings" or similar, with at least one row showing Q30 had ~1,537 labeled values decoded. If the section is absent or empty, the warnings aren't being plumbed through.

STEP 7. Report results
Paste back to me:
- pytest summary line
- Streamlit startup status (clean or error)
- Wall-clock time from STEP 4
- Full output from STEP 5 (with VERDICT line)
- Full output from STEP 6 (first 30 warning rows)

CONSTRAINTS

- Do not modify any files I uploaded. If something doesn't work, paste the error and stop.
- Keep the existing app.py fix at line 1410 (with app.container() instead of nested expander) — this Round 4 should not touch app.py at all. If app.py was uploaded by Codex, check that line 1410 is still 'with app.container():' before restarting Streamlit.
- Do not delete older outputs/*.xlsx files — we keep them for comparison.
- If Streamlit hits the same UnboundLocalError on 'app' that Round 3 had, check whether Codex's new excel_exporter.py somehow introduced a similar issue. Paste the traceback.

Verify the existing app.py line 1410 fix is intact:

cd artifacts/survey-insight-engine && sed -n '1408,1412p' app.py

That must show 'with app.container():' — NOT 'with app.expander(button_label, expanded=False):'. If it shows the expander, re-apply: sed -i 's/        with app.expander(button_label, expanded=False):/        with app.container():/' app.py