COMBINED 5X + 5Y + 5Z DEPLOY — Replit
cd ~/workspace/artifacts/survey-insight-engine
mkdir -p ~/.streamlit && printf '[general]\nemail = ""\n' > ~/.streamlit/credentials.toml

STEP 1 — new code landed
grep -n "if widget_key not in app.session_state" app.py            # 5Z chart fix present
grep -cn "app.session_state\[widget_key\] = selected_type" app.py  # expect 1 (only inside the guard)
grep -n "_DIFF_INSIGHTS_PER_PAGE = 10\|_validated_outcome_diff_insight\|Next 10" app.py | head   # 5Z feed+guard
grep -cn "_section_hypothesis_check()\|TEMP DEBUG" app.py            # 5X: expect 0
grep -n "_wvl_compute_multi_select_cohort_results\|respondents_who_answered_any" src/excel_exporter.py | head  # 5Y
grep -n "_compute_multi_select_cohort_results\|respondents_who_answered_any" src/outcome_segmentation.py | head # 5Y

STEP 2 — prior rounds intact
grep -n "reconcile_multiselect_value_subtypes" src/question_classifier.py | head     # 5S
grep -n "_build_multi_select_respondent_count_formula" src/excel_exporter.py | head   # 5V single-cut denom
grep -n "_grid_spec_subtype" src/excel_exporter.py | head                             # 5T

STEP 3 — protective markers
grep -c "_run_with_status_heartbeat" app.py            # ~6
grep -n "ThreadPoolExecutor(max_workers=3)" app.py

STEP 4 — install + tests
pip install -r requirements.txt
python -m pytest tests/test_chart_type_override.py tests/test_excel_exporter_winners_vs_laggards.py tests/test_outcome_segmentation.py -q
python -m pytest -q

STEP 5 — launch + liveness
pkill -9 -f streamlit; sleep 1
nohup streamlit run app.py --server.port 21049 --server.address 0.0.0.0 --server.headless true >/tmp/st.log 2>&1 &
sleep 6; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:21049   # expect 200

STEP 6 — THE GATE: validate 5Y on REAL data (still the actual approval for 5Y)
- Upload the real CE file (winvslag2024.xlsx — has genuine multi-OPTION questions).
- Run outcome segmentation, AI OFF, download the W/L workbook.
- HARD CHECK on any multi-OPTION multi-select: cohort % must be strictly 0<x<100 (NOT pinned at
  100%), and "Winners total" must be >= the largest single option count. If any multi-option
  question shows 100% across the board or Winners total == one option's count, the denominator fix
  did NOT take — STOP and report.
- If M01_medium.xlsx + the validator exist on Replit, run it: expect cross-cut 72/72,
  AI grounding 21/21, single-cut 113/113.
- Send me the regenerated REAL W/L workbook (+ AI transcript) — I'll diff cohort %s vs ground truth.

STEP 7 — human eyeball
- Chart type: pick a non-default type → it sticks AND the chart redraws (the 5Z fix).
- AI Analysis: per-differentiator cards with AI headlines, right-side winner/laggard/gap/lift
  metrics, "Show source table" highlights the right row, Next/Previous 10 pages cleanly.
- Toggle AI OFF → cards show the Python-template headline (was_template) with correct numbers.
- Single-cut: spot-check Q45 (genuine binary grid) still renders right.
- Don't change global filters mid-session.