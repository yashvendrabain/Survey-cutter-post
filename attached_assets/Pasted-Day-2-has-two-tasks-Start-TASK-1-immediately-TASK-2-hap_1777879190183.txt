Day 2 has two tasks. Start TASK 1 immediately. TASK 2 happens 
when I paste Codex's files.

PATH NOTE: All commands use 
  cd artifacts/survey-insight-engine
not 
  cd survey_insight_engine
The sample files should be at:
  artifacts/survey-insight-engine/sample_data/Datamap_sample.xlsx
  artifacts/survey-insight-engine/sample_data/Rawdata_sample.xlsx
Confirm both exist before running TASK 2.

TASK 1 — Save the PRD into the project (do this now)

Create the file artifacts/survey-insight-engine/docs/PRD.md.
I will paste the PRD contents in my next message. Save the file 
verbatim, do not modify any content.

Then commit:
  "Day 2: add PRD"

Reply when done so I can paste the PRD content.

TASK 2 — Run datamap parser on real sample (waits for Codex)

When Codex delivers, I will paste three files: 
src/datamap_parser.py, tests/conftest.py, and 
tests/test_datamap_parser.py. You will:

1. Save each file at the correct path in 
   artifacts/survey-insight-engine/. 
   Replace any existing placeholder content.

2. Run the unit tests:
     cd artifacts/survey-insight-engine
     python -m unittest tests.test_datamap_parser -v
   Paste the full output.

3. Run the parser against the real sample datamap:

     cd artifacts/survey-insight-engine && python -c "
     from src.datamap_parser import parse_datamap
     from collections import Counter

     dm = parse_datamap('sample_data/Datamap_sample.xlsx')

     print('=== TOTALS ===')
     print(f'Questions parsed: {len(dm[\"questions\"])}')
     print(f'Total rows in Sheet1: {dm[\"total_rows_in_sheet\"]}')
     print(f'Parser warnings: {len(dm[\"parser_warnings\"])}')
     for w in dm['parser_warnings'][:10]:
         print(f'  WARN: {w}')

     print()
     print('=== TYPE HINT BREAKDOWN ===')
     types = Counter(q['type_hint'] for q in dm['questions'])
     for t, n in sorted(types.items(), key=lambda x: str(x[0])):
         print(f'  {t}: {n}')

     print()
     print('=== STRUCTURE BREAKDOWN ===')
     has_opts = sum(1 for q in dm['questions'] if q['options'])
     has_subs = sum(1 for q in dm['questions'] 
                    if q['sub_columns'])
     has_both = sum(1 for q in dm['questions'] 
                    if q['options'] and q['sub_columns'])
     print(f'  has options:               {has_opts}')
     print(f'  has sub_columns:           {has_subs}')
     print(f'  has both (likely grids):   {has_both}')

     print()
     print('=== OPEN-TEXT FOLLOW-UPS ===')
     oe = [q for q in dm['questions'] 
           if q['parent_canonical_id']]
     print(f'  count: {len(oe)}')
     for q in oe[:5]:
         print(f'  {q[\"canonical_id\"]} -> '
               f'parent {q[\"parent_canonical_id\"]}')

     print()
     print('=== QUESTIONS WITH WARNINGS ===')
     warned = [q for q in dm['questions'] if q['warnings']]
     print(f'  count: {len(warned)}')
     for q in warned[:10]:
         print(f'  {q[\"canonical_id\"]} (row '
               f'{q[\"source_row\"]}): {q[\"warnings\"]}')

     print()
     print('=== SAMPLE QUESTIONS ===')
     for q in dm['questions'][:5]:
         print(f'  {q[\"canonical_id\"]:20s}  '
               f'type={q[\"type_hint\"]!s:15s}  '
               f'opts={len(q[\"options\"])}  '
               f'subs={len(q[\"sub_columns\"])}')
     "

   Paste the full output.

4. If anything errors, do NOT modify the parser. Paste the error 
   and the command exactly as run.

CONSTRAINTS
- Do not modify any file Codex authored.
- Do not modify src/models.py.
- Do not write parsing logic yourself.

COMMIT MESSAGES
  "Day 2: add PRD"
  "Day 2: integrate datamap parser, run real-file verification"

WHEN YOU FINISH

Reply with:
- TASK 1 done confirmation
- (later) TASK 2 unittest output (full)
- (later) TASK 2 real-file output (full)
- any errors or surprises