# Replit Prompt — Three-bug fix verification (Final round)

Codex returned the three-bug fix. Twelve files modified (calculators got a shared `_conditional.py` helper, plus the parser, models, classifier, and tests). Local test count: 464 (up from 457 — they added new tests for the fixes).

## Files to apply

Replace these with Codex's latest versions:
- `src/models.py`
- `src/question_classifier.py`
- `src/datamap_parser.py`
- `src/single_cut/_conditional.py` (NEW file)
- `src/single_cut/_grid.py`
- `src/single_cut/_multi_select.py`
- `src/single_cut/_numeric.py`
- `src/single_cut/_single_select.py`
- `src/single_cut/grid_binary_pivot.py`
- `src/single_cut/grid_rated.py`
- `src/single_cut/rank_order.py`
- `tests/test_datamap_parser.py`
- `tests/test_grid_binary_pivot.py`
- `tests/test_grid_rated.py`
- `tests/test_rank_order.py`

Do not modify Codex's files.

## Verification — revised bar

The success criteria are different this time. We're NOT trying to match the inspo file's exact numbers (that's impossible without replicating consultant-applied filters that aren't documented in the datamap). We're verifying that the three bugs are fixed.

### Step 1 — Test suite

```bash
python -m pytest tests/ -v 2>&1 | tail -30
```

Expected: 464 + 3 = 467 passing in your sandbox (you have 3 extra FilterSpec tests that Codex doesn't have). 0 failing.

If any test fails, paste the failure. Don't fix it.

### Step 2 — Run engine on BCN data (same subset approach as last time, full BCN exceeds the sandbox timeout)

```python
from pathlib import Path
import pickle
from src.question_classifier import classify_questions
from src.single_cut.engine import compute_single_cuts
from src.calculation_log import CalculationLog
from src.models import SurveySchema

# Reload the saved BCN data + schema
with open('outputs/_bcn_raw_df.pkl', 'rb') as f:
    decoded_df = pickle.load(f)
with open('outputs/_bcn_data_map.pkl', 'rb') as f:
    data_map = pickle.load(f)

# Re-classify (this picks up the new parser/classifier behavior)
schema = classify_questions(data_map, set(decoded_df.columns))

# Subset schema to just the 9 spot-check questions
target_ids = {"Q22", "Q26", "Q30", "Q33", "Q34", "Q35", "Q36", "Q41", "Q43"}
subset_questions = tuple(q for q in schema.questions if q.canonical_id in target_ids)
subset_schema = SurveySchema(
    questions=subset_questions,
    respondent_id_column=schema.respondent_id_column,
    total_respondents=schema.total_respondents,
    source_datamap_path=schema.source_datamap_path,
    source_rawdata_path=schema.source_rawdata_path,
    parsed_at=schema.parsed_at,
)

log = CalculationLog()
results, skips = compute_single_cuts(subset_schema, decoded_df, log)
print(f'Results: {len(results)}, Skips: {len(skips)}')
print()

# Show per-question key numbers
for r in results:
    qid = r.question_id
    rtype = type(r).__name__
    print(f'--- {qid} ({rtype}) ---')
    print(f'  total_respondents (denominator): {r.valid_n}')
    
    if hasattr(r, 'column_headers'):
        print(f'  column_headers: {r.column_headers}')
    if hasattr(r, 'rows') and r.rows:
        # Show first 2 rows
        for row in r.rows[:2]:
            row_id = getattr(row, 'row_id', getattr(row, 'option_id', '?'))
            if hasattr(row, 'counts_per_column'):
                print(f'  {row_id}: counts={row.counts_per_column}')
            elif hasattr(row, 'means_per_column'):
                print(f'  {row_id}: means={[round(m, 2) for m in row.means_per_column]}')
            elif hasattr(row, 'counts_per_rank'):
                print(f'  {row_id}: ranks={row.counts_per_rank}')
    print()
```

### Step 3 — Verify the three bug fixes

**Bug 1 — Q26 / Q41 column count and headers:**

- Q26 column_headers should now have **5 entries** (not 10)
- The labels should be role names like "Blocked Vendors", "Scored Vendors", "Recommended for/against Vendors", "Restricted...", "Others"
- The labels should NOT include "Checked", "c2", "c3", "c4", "c5"
- Same expectation for Q41

**Verification line for Q26r11:**
- `counts_per_column[0]` should be 23 (matches inspo's first c-column count)

**Bug 2 — conditional_on actually applied:**

For each question with conditional_on='Q28' (Q36, Q43, plus any recovered by the parser):
- `valid_n` (total_respondents) should be ≤ Q28's non-null count (638)
- For Q43, the previous unfiltered value was ~755; after the fix it should drop to ≤ 638

**Verification: print conditional_on for each target question:**

```python
print("\n--- conditional_on values ---")
for qid in ["Q22", "Q26", "Q30", "Q33", "Q34", "Q35", "Q36", "Q41", "Q43"]:
    spec = subset_schema.get_question(qid)
    if spec:
        print(f"  {qid}: conditional_on={spec.conditional_on!r}")
```

Expected:
- Q22, Q34, Q35 — should now be `None` OR a real ID (the bogus tokens like 'Q22_text' should be cleared)
- Q36, Q43 — should remain 'Q28'
- Q26, Q30, Q33, Q41 — may have been recovered to 'Q28' by the parser's recovery pass (if their question_text contains a pipe marker), or stay None

**Bug 3 — no invalid tokens in the schema:**

Run a quick check:

```python
canonical_ids = {q.canonical_id for q in schema.questions}
bogus = [(q.canonical_id, q.conditional_on) for q in schema.questions
         if q.conditional_on and q.conditional_on not in canonical_ids]
print(f"\nInvalid conditional_on values: {bogus}")
```

Expected: empty list `[]`. Any token that doesn't resolve to a real canonical_id is a parser bug.

### Step 4 — Final paste-back

Reply with:

```
Step 1 — Tests: X passing, Y failing
Step 2 — Engine run: Results=X, Skips=Y, types=[...]
Step 3a — Q26 column count: 5 / N
   Q26 column_headers: [...]
   Q26r11 counts: [...]
Step 3b — conditional_on values:
   Q22: ...
   Q26: ...
   ...
Step 3c — Invalid conditional_on tokens: []  / [list]
Step 3d — Q43 total_respondents: N (should be ≤ 638)
```

## What this completes

If all three bugs are fixed (Q26 has 5 columns with role-name labels, Q43 denominator ≤ 638, no invalid tokens in schema) → the classifier overhaul is fully verified and ready to ship.

The output will not match the inspo file's exact numbers — and that's intentional. The inspo uses additional consultant-applied filters not documented in the datamap. Our tool produces correct raw cuts plus documented conditional_on filtering. Custom views like the inspo's "Special Cut #1" are produced through the filter/cross-cut UI.

## Constraints

- Don't touch app.py, excel_exporter.py, or AI files
- Don't try to match inspo numbers exactly — that's not the success criteria
- If any test fails, paste the failure
- If any spot-check diverges from expected, paste the actual values

We're at the finish line. After this verification, we move on to the next priority.