STAGE G FINAL FIX — Install the chi-square test fixture correction.

CONTEXT

Codex's previous Stage G round had one remaining test failure: test_run_chi_square_cramer_v failed because its 2x2 fixture triggered a Yates correction mismatch between chi2_contingency and contingency.association. That was a test-side bug, not a runtime bug — chi-square hypotheses already work correctly in the browser.

This round changes ONLY:
1. tests/test_hypothesis_validator.py — test fixture switches from 2x2 to 3x3 table
2. src/hypothesis_validator.py — one comment line added to _run_chi_square

No runtime behavior changes. No model, exporter, or app.py touched.

FILES BEING UPLOADED

1. src/hypothesis_validator.py — replaces existing. Single change: explanatory comment above the chi2_contingency call.

2. tests/test_hypothesis_validator.py — replaces existing. Single change: test_run_chi_square_cramer_v uses a 3x3 table.

INSTRUCTIONS

STEP 1. Defensive check — confirm models.py hasn't been touched.

cd artifacts/survey-insight-engine && grep -n "def get_effective_values" src/models.py

Must return one match. If zero, escalate.

STEP 2. Install both files.

STEP 3. Run the previously failing test.

pytest tests/test_hypothesis_validator.py::TestHypothesisValidator::test_run_chi_square_cramer_v -v 2>&1 | tail -10

Must pass.

STEP 4. Run the full hypothesis validator suite.

pytest tests/test_hypothesis_validator.py -v 2>&1 | tail -10

All tests must pass.

STEP 5. Run the full suite chunked as usual.

Expected: ~527-540 total, 0 failures (plus existing 3 Plotly skips).

STEP 6. Restart Streamlit.

cd artifacts/survey-insight-engine && streamlit run app.py

Verify clean startup. No code changes in production paths — startup should be uneventful.

STEP 7. Verify protective app.py fixes still in place.

sed -n '1408,1413p' app.py
grep -n "^def _run_pipeline\|app = _require_streamlit" app.py | head -10

Line 1410 must show "with app.container():" and _run_pipeline must have the app = _require_streamlit() at the top. Codex didn't re-upload app.py this round, so these should be untouched. If they aren't, re-apply previous fixes.

REPORT BACK

Paste:
- STEP 1 result
- STEP 3 result
- STEP 4 result (with the new test count)
- STEP 5 chunked summary
- STEP 6 startup status
- STEP 7 results

CONSTRAINTS

- Do not modify any uploaded file.
- Do not modify any other file unless STEP 7 directs you to.