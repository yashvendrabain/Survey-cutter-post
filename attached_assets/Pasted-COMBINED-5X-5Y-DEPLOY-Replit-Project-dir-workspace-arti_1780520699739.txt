COMBINED 5X + 5Y DEPLOY — Replit
Project dir: ~/workspace/artifacts/survey-insight-engine

STEP 0 — setup
cd ~/workspace/artifacts/survey-insight-engine
mkdir -p ~/.streamlit && printf '[general]\nemail = ""\n' > ~/.streamlit/credentials.toml

STEP 1 — confirm new code landed
# 5X (app.py)
grep -n "_sorted_differentiators_for_display\|_style_differentiator_source\|Show more insights\|_section_header(\"5\", SECTION_DOWNLOADS" app.py
grep -cn "_section_hypothesis_check()" app.py          # expect 0
grep -cn "TEMP DEBUG\|DEBUG_run_complete" app.py        # expect 0
# 5Y (exporter + segmentation)
grep -n "_wvl_compute_multi_select_cohort_results\|respondents_who_answered_any" src/excel_exporter.py | head
grep -n "_compute_multi_select_cohort_results\|respondents_who_answered_any" src/outcome_segmentation.py | head

STEP 2 — confirm prior rounds still intact (regression guard)
grep -n "reconcile_multiselect_value_subtypes" src/question_classifier.py | head    # 5S
grep -n "_build_multi_select_respondent_count_formula" src/excel_exporter.py | head # 5V single-cut denom
grep -n "_grid_spec_subtype" src/excel_exporter.py | head                           # 5T

STEP 3 — protective markers
grep -c "_run_with_status_heartbeat" app.py            # expect ~6
grep -n "ThreadPoolExecutor(max_workers=3)" app.py     # parallel Stage-4 AI

STEP 4 — install + tests
pip install -r requirements.txt
python -m pytest tests/test_excel_exporter_winners_vs_laggards.py tests/test_outcome_segmentation.py -q
python -m pytest -q     # full suite

STEP 5 — launch + liveness
pkill -9 -f streamlit; sleep 1
nohup streamlit run app.py --server.port 21049 --server.address 0.0.0.0 --server.headless true >/tmp/st.log 2>&1 &
sleep 6; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:21049   # expect 200

STEP 6 — THE GATE: validate 5Y on REAL data (not synthetic)
- Upload the real CE file (winvslag2024.xlsx) — it has genuine multi-OPTION questions.
- Run outcome segmentation with the real winner/laggard codes, AI OFF, download the W/L workbook.
- HARD CHECK on any MULTI-OPTION multi-select question:
    * a cohort % must be strictly between 0 and 100 (NOT pinned at 100% across the board)
    * "Winners total" must be >= the largest single option count (it's the answered-any count,
      not equal to one option's count)
  If a multi-option question shows 100% everywhere OR Winners total == a single option's count,
  the denominator fix did NOT take effect on the real path — STOP and report.
- If M01_medium.xlsx + the validator are present on Replit, run it: expect cross-cut 72/72,
  AI grounding 21/21, single-cut 113/113.
- Send me the regenerated REAL W/L workbook (+ AI transcript). I'll diff cohort %s against
  ground truth the same way I did the rank/allocation fixes.

STEP 7 — human eyeball
- Segmentation UI: no Winner Profile, no Hypothesis section, Downloads = section 5, no debug JSON,
  differentiators sorted by lift then gap, "Show more insights" works, "Show source table"
  highlights the top differentiator's row + Lift/winner-rate cells.
- Single-cut: spot-check Q45 (the genuine binary grid) still renders correctly.
- Do NOT change global filters mid-session (refresh isn't hardened).