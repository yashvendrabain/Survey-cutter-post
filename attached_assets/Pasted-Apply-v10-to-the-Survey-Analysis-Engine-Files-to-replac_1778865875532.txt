Apply v10 to the Survey Analysis Engine.
Files to replace from Codex:

src/excel_exporter.py — full replacement (filter label fix, heading restoration, Bain red styling)
tests/test_excel_exporter.py — updated tests

What this iteration ships:

Filter labels now come from src.ai_insights.generate_short_labels() instead of broken keyword detection. Demographic filters on the Filters sheet should show clean 2-3 word labels.
Question heading rows on theme sheets now show "Q1 - Question text" format (Q-prefix restored from v9 strip).
Question heading rows now styled with Bain red #CC0000 fill + white bold Arial 11 text.

Run after replacing:

pytest (full suite) — Codex reported 375 passes. Confirm and report. Note: this is 3 less than v9's 378. Investigate why if the delta is unexpected (tests deleted/replaced vs added).
Verify Portkey is reachable: python -c "import os; print('Portkey key set:', bool(os.environ.get('PORTKEY_API_KEY')))". If False, set the key in Replit Secrets before regenerating workbooks.
Generate a fresh workbook against sample_data/Rawdata_sample.xlsx + sample_data/Datamap_sample.xlsx and save to outputs/survey_v10_labels.xlsx.
Generate a second workbook against the BCN file and save to outputs/survey_v10_bcn.xlsx.

Verification — confirm all checks:

AI labels on Filters sheet (sample file): Column A shows clean labels like "Country", "Industry", "Company Size" (whatever the sample data demographics are). No "Which Of The Following Best" or 5-word truncations.
AI labels on Filters sheet (BCN file): Same — Q4 should show something like "Org Size" not "Which Of The Following Best". Q14 should be "Org Age" or similar, not "Approximately How Old Is The". Three different role-related demographics should NOT all be labeled "Role".
Q-prefix restored: Theme sheet question headings show "Q1 - In which of the following countries..." format.
Bain red styling: Question heading rows have red background with bold white text. Confirm visually.
Memory-safety mode still works on BCN: BCN completes without OOM. Run_Summary still shows the memory-safety notice. (Filters won't propagate on BCN — that's by design.)
Regression — sample file filters still propagate: Open the small sample workbook, set a Country filter on the Filters sheet, confirm Q17 counts change.

If AI labels look like 40-char truncations ("Approximately how old is the org..."): that means generate_short_labels is hitting its template fallback because Portkey is unreachable. Diagnose with: python -c "from src.ai_insights import generate_short_labels; print(generate_short_labels([{'question_id': 'Q4', 'question_text': 'Which of the following best describes the size of your organization, including all locations worldwide?'}]))". Should print something like {"Q4": "Org Size"}. If it prints {"Q4": "Which of the following best describes ..."} truncated, Portkey is failing — paste any error.
Report: test count, all verification checks (pass/fail), and side-by-side sample labels for BCN's Q4, Q7, Q14, Q32 demographic questions.