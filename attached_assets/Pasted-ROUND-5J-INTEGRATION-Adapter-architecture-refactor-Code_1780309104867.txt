ROUND 5J INTEGRATION — Adapter architecture refactor.

Codex shipped:
- 5 new files in src/adapters/ (base, registry, bcn_multicolumn, compact_two_column, label_pattern_subcolumn)
- src/datamap_parser.py becomes thin wrapper
- src/question_classifier.py imports label-pattern utility from adapters
- tests/test_subcolumn_label_pattern.py modified (likely import updates)
- 4 new test files: test_adapter_base.py, test_bcn_multicolumn_adapter.py, test_compact_two_column_adapter.py, test_adapter_router.py
- 667 tests, 0 failures locally

CRITICAL — this is a refactor. All previously-passing functionality must still work identically. Two pre-install verifications needed.

INSTRUCTIONS

STEP 0. Defensive checks on production files BEFORE install.

cd artifacts/survey-insight-engine && grep -n "def get_effective_values" src/models.py
grep -n "label_to_numeric_value\|allowed_numeric_range\|manual_cohort_id_column" src/models.py
sed -n '1625,1635p' app.py
grep -n "^def _run_pipeline\|app = _require_streamlit" app.py | head -5

All must return matches. None of these files are touched this round.

STEP 0.5 — CRITICAL: Inspect what test_subcolumn_label_pattern.py was changed to.

cd artifacts/survey-insight-engine && grep -n "def test_" attached_assets/test_subcolumn_label_pattern_*.py 2>/dev/null | wc -l
grep -n "def test_" tests/test_subcolumn_label_pattern.py 2>/dev/null | wc -l

The Round 5I version had 11 tests. The incoming version should have approximately the same count (modifications should be import updates, not test deletions). If the incoming file has fewer than 9 tests, STOP and report — Codex may have silently removed coverage.

STEP 0.7 — CRITICAL: Verify the keystone regression test exists.

grep -n "def test_" attached_assets/test_adapter_router_*.py 2>/dev/null

Look for a test name matching one of these patterns:
- test_router_output_identical_to_legacy
- test_bcn_sample_parses_identically
- test_winvslag2024_parses_identically
- test_regression_against_baseline

If NONE of these patterns exist in test_adapter_router.py, the refactor lacks its critical regression test. The prompt explicitly required this. Flag it before installing.

STEP 1. Install all uploaded files.

After install, re-verify markers landed:

grep -rn "class BcnMulticolumnAdapter\|class CompactTwoColumnAdapter\|apply_label_pattern_matching" src/adapters/
grep "from src.adapters" src/datamap_parser.py src/question_classifier.py

All references must exist.

STEP 1.5. Apply path fix to new test files if needed.

grep "APP_PATH = Path" tests/test_adapter_*.py tests/test_bcn_multicolumn_adapter.py tests/test_compact_two_column_adapter.py 2>/dev/null

If any has the doubled "artifacts/survey-insight-engine" path, apply:

sed -i 's|parents\[1\] / "artifacts" / "survey-insight-engine" / "app.py"|parents[1] / "app.py"|g' tests/test_adapter_*.py tests/test_bcn_multicolumn_adapter.py tests/test_compact_two_column_adapter.py 2>/dev/null

(May not be needed — these tests probably don't reference app.py.)

STEP 2. Run all new adapter tests.

pytest tests/test_adapter_base.py tests/test_bcn_multicolumn_adapter.py tests/test_compact_two_column_adapter.py tests/test_adapter_router.py tests/test_subcolumn_label_pattern.py -v 2>&1 | tail -60

Expected: ~33-40 tests pass total. NO failures.

If test_subcolumn_label_pattern.py has fewer than 9 tests, REPORT that.

STEP 3. Run pre-existing parsing/classifier regression tests.

pytest tests/test_compact_datamap_parser.py tests/test_question_classifier.py tests/test_respondent_id_detection.py tests/test_datamap_parser.py -v 2>&1 | tail -25

Expected: all pass. These tests pre-existed; the refactor must not break them.

STEP 4. Full suite chunked.

Expected: ~667 total, 0 failures.

STEP 5. Verify protective fixes (defensive — should be untouched).

sed -n '1625,1635p' app.py
grep -n "^def _run_pipeline" app.py | head -3

`with app.container():` must remain. _run_pipeline must have `app = _require_streamlit()` early.

STEP 6. Restart Streamlit.

cd artifacts/survey-insight-engine && streamlit run app.py

Verify clean startup, no ImportError. Most likely failure mode is a circular import involving src/adapters/* and src/datamap_parser.py — paste any traceback.

STEP 7. CRITICAL — end-to-end browser smoke test on THREE files.

Hard-refresh browser. For EACH file below, upload, verify the wizard shows non-zero questions, and complete a single-cut analysis:

7a. Datamap_sample.xlsx + Rawdata_sample.xlsx (Scenario A — separate files)
    - Expected: ~50 questions classified, single-cut analysis completes, Q14 shows correct counts
    
7b. winvslag2024.xlsx (Scenario B — combined file with compact datamap)
    - Expected: questions parsed (~80+ from Round 5H+5I work), Q3 shows MULTI_SELECT_BINARY with 6 options
    
7c. BCN_LTB_raw_data-_3_9_26_EOD_.xlsx (Scenario B — combined file with bracketed-header datamap)
    - Expected: questions parsed, Q8r1-Q8r7 detected as sub-columns of Q8
    
Tell me what question count you see for each.

REPORT BACK

- STEP 0/0.5/0.7 verification results
- STEP 1 marker verification
- STEP 2 adapter test results
- STEP 3 regression test results
- STEP 4 full suite total
- STEP 5 protective fixes
- STEP 6 Streamlit startup
- STEP 7 results for ALL THREE files (question count + Q3/Q14/Q8 behaviors)

CONSTRAINTS

- Do not modify any uploaded file unless STEP 1.5 path fix is needed.
- If STEP 0.5 shows test count shrunk from 11 to <9, stop and report.
- If STEP 0.7 doesn't find a regression test, stop and report.
- If STEP 7 shows ANY of the three files producing fewer/different questions than the pre-5J baseline, the refactor regressed.