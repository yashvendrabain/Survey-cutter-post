Confirmed bug: user clicked the legacy centered "Run analysis" button, not the wizard's Step 5 button. Apply this surgical fix:
At line 2825-2832 (the legacy button rendering), wrap the entire centre_left, centre_mid, centre_right = app.columns(...) block with a condition that hides it while the wizard is active.
Find this in app.py:
python    # -- Setup wizard (appears after upload, before Run Analysis) --
    if uploaded_files and not app.session_state.get("wiz_complete") and not app.session_state.get("run_complete"):
        _render_setup_wizard(uploaded_files)

    ready = bool(uploaded_files) and not docx_only
    centre_left, centre_mid, centre_right = app.columns([2, 3, 2])
    with centre_mid:
        run_clicked = app.button(
            "Run analysis",
            type="primary",
            disabled=not ready,
            use_container_width=True,
        )
Change to:
python    # -- Setup wizard (appears after upload, before Run Analysis) --
    wizard_active = uploaded_files and not app.session_state.get("wiz_complete") and not app.session_state.get("run_complete")
    if wizard_active:
        _render_setup_wizard(uploaded_files)

    ready = bool(uploaded_files) and not docx_only
    run_clicked = False
    if not wizard_active:
        centre_left, centre_mid, centre_right = app.columns([2, 3, 2])
        with centre_mid:
            run_clicked = app.button(
                "Run analysis",
                type="primary",
                disabled=not ready,
                use_container_width=True,
            )
Then the existing if app.session_state.pop("_wizard_run_requested", False): run_clicked = True stays as-is on the next line.
Watch out for CRLF line endings (your edit tool failed earlier on this file — use Python to patch).
Restart Streamlit. Confirm no syntax errors.
Removable diagnostics: while you're in there, remove the WIZ_APPLY_CALLED and WIZ_OVERRIDE_READ logging lines too (the bug is found, we don't need them anymore). And the diagnostic line at 2823-2824 (logging.warning("RUN_CLICKED uploaded=%s...")).
Report back when done.