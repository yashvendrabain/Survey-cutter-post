Day 11 standby. Codex is building filter system logic only — 
no UI changes today. Your job: save Codex's files, run all 
tests, run the real-file sanity check.

PATH: cd artifacts/survey-insight-engine

When Codex delivers, save these files:
  - src/models.py (UPDATED — adds is_demographic to 
    QuestionSpec, new FilterSpec, new FilteredSingleCutResult)
  - src/question_classifier.py (UPDATED)
  - src/filtered_single_cut.py (NEW)
  - tests/test_filtered_single_cut.py (NEW)
  - tests/test_question_classifier.py (UPDATED)

Run tests:
  cd artifacts/survey-insight-engine
  python -m unittest tests.test_filtered_single_cut -v
  python -m unittest tests.test_question_classifier -v
  python -m unittest tests.test_models -v
  python -m unittest tests.test_calc_primitives -v
  python -m unittest tests.test_single_select -v
  python -m unittest tests.test_multi_select -v
  python -m unittest tests.test_numeric -v
  python -m unittest tests.test_grid -v
  python -m unittest tests.test_single_cut_engine -v
  python -m unittest tests.test_cross_cut_engine -v
  python -m unittest tests.test_cross_cut_suggestions -v
  python -m unittest tests.test_excel_exporter -v

Paste each output. Stop here if any fail.

Then run the real-file sanity check (Codex specifies the 
exact script in their prompt). Paste output.

What we're looking for:
  - At least 5 demographic questions detected on the real 
    file (region, country, industry, function, role, etc.)
  - Filter-with-value test produces a single_cut_result 
    with sensible filtered_n
  - Filter-without-value test produces a cross_cut_result 
    with per-segment breakdown

If demographic detection finds zero questions, paste a list 
of all SINGLE_SELECT questions in the schema with their text 
so I can adjust keywords for Day 12.

CONSTRAINTS
- Do NOT modify any file Codex authors.
- Do NOT touch app.py today (UI work is Day 12).
- Do NOT modify config.py.

COMMIT MESSAGE
"Day 11: filter system saved + verified"