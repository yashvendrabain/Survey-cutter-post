Wire the new live-filter Excel exporter into app.py.

Codex delivered:
- src/ai_insights.py — added categorize_demographic_questions()
- src/excel_exporter.py — rebuilt around live COUNTIFS/AVERAGEIFS
- tests/test_ai_insights.py — added demographic-priority test
- tests/test_excel_exporter.py — updated for new workbook shape
- 339 tests passing

STEP 1 — REPLACE FILES VERBATIM

Replace all 4 files from Codex attachments.

STEP 2 — APPLY THE DEMOGRAPHIC PRIORITY SNIPPET

Find both existing export call sites in app.py (where 
export_single_cuts is called):

    grep -n "export_single_cuts\|categorize_questions_into_themes" app.py

At BOTH call sites (typically _run_pipeline and _refresh_full_workbook),
the existing wiring already builds themes. NOW also build demo_priority 
right before the export_single_cuts call. Apply this pattern AT BOTH 
SITES:

    from src.ai_insights import (
        categorize_questions_into_themes,
        categorize_demographic_questions,
    )
    
    # Existing themes wiring stays as-is
    questions_for_themes = [
        {
            "question_id": q.canonical_id,
            "question_text": q.question_text,
            "question_type": q.question_type.value,
            "is_demographic": q.is_demographic,
        }
        for q in schema.questions
    ]
    themes = categorize_questions_into_themes(
        questions_for_themes,
        cache=_INSIGHT_CACHE,
    )
    
    # NEW: demographic priority wiring
    demographic_questions_for_priority = [
        {
            "question_id": q.canonical_id,
            "question_text": q.question_text,
            "question_type": q.question_type.value,
        }
        for q in schema.demographic_questions()
    ]
    demo_priority = categorize_demographic_questions(
        demographic_questions_for_priority,
        cache=_INSIGHT_CACHE,
    )
    
    # Updated export call — add demo_priority parameter
    export_single_cuts(
        results=results,
        skips=skips,
        schema=schema,
        quality_report=quality_report,
        log=log,                   # or audit_log depending on scope
        output_path=output_path,
        cross_cut_results=cross_cut_results,
        cross_cut_skips=cross_cut_skips,
        themes=themes,
        decoded_df=dataframe,      # or decoded_df depending on scope
        demo_priority=demo_priority,
    )

Use whichever variable names already exist in each call site's scope.

STEP 3 — RUN TESTS

pytest --tb=short -q

Expected: 339 tests pass. If any fail, paste full error.

STEP 4 — SMOKE TEST

1. Upload sample data, run analysis
2. Click workbook download
3. Open the .xlsx in Excel
4. Verify and report:

WORKBOOK STRUCTURE:
- Click View > Unhide Sheet. Confirm _RawData and _Options are 
  listed (means they were written but hidden). 
- Cancel the unhide dialog without unhiding.
- List the visible sheet names.

THEME SHEET CHECK (pick any theme):
- Demographic filter row visible at top with dropdown arrows? (yes/no)
- Click an Industry dropdown — do real industry options appear 
  (e.g. Software, Machinery)? (yes/no)
- Are there options shown by their LABELS not codes (1, 2, 3)? (yes/no)

LIVE FILTER TEST (most important):
- Open any theme sheet, note the count for Q18 / "6-9% growth" 
  in unfiltered view. Paste the value.
- Pick Industry = Software from the demographic filter dropdown
- Did the count for "6-9% growth" change? Paste the new value.
- Did ALL question tables on the sheet update simultaneously? (yes/no)
- Did the % column also update? (yes/no)

PER-QUESTION FILTER TEST:
- Find the per-question filter row above Q18
- Click the question dropdown — does it list other questions? (yes/no)
- Pick a question (e.g. Q2 Country), then pick a value (e.g. India)
- Did Q18's table update to show only India respondents? (yes/no)
- Did OTHER question tables remain unaffected? (yes/no — this is 
  important; per-question filter should only affect its own table)

CROSS-TAB MINI-TABLE:
- Above Q18, is there a "Cross-tab by" dropdown? (yes/no)
- Pick "Q5_Industry" from it
- Does a mini-table appear next to the Q18 distribution showing 
  Q18 × Industry breakdown? (yes/no)

CUSTOM FILTERS:
- Custom Filter 1 — pick a question and value
- Do all tables on the sheet update? (yes/no)

DEMOGRAPHIC ORDERING:
- Are demographic filter columns ordered by priority? (Country 
  should appear before Industry, Industry before Company Size, etc.)
- Paste the order of demographic columns left to right.

NUMERIC ALLOCATION:
- Find a numeric allocation question in any theme sheet
- Does it show Mean, Min, Max? (yes/no)
- Does it show Median? (should NOT — was removed)
- Pick a demographic filter, does Mean update? (yes/no)

ANY ERRORS:
- Python exceptions during export?
- Excel "corrupt workbook" warnings?
- #REF!, #NAME?, #VALUE! errors in any cells?

Report all sections.