ROUND 5B INTEGRATION — Winners vs Laggards workbook presentation fixes.

Codex shipped formatting and filter UI rows for the WVL workbook. 2 files: src/excel_exporter.py and tests/test_excel_exporter_winners_vs_laggards.py. No models, no app.py, no other modules.

INSTRUCTIONS

STEP 0. Defensive checks BEFORE install.

cd artifacts/survey-insight-engine && grep -n "def get_effective_values" src/models.py
grep -n "label_to_numeric_value\|allowed_numeric_range" src/models.py

Both must return matches. models.py was not touched this round, so this is just defensive confirmation.

STEP 1. Install both files.

STEP 2. Run the WVL tests specifically.

pytest tests/test_excel_exporter_winners_vs_laggards.py -v 2>&1 | tail -25

Expected: 14 tests pass.

STEP 3. Run full suite chunked.

Expected: ~566 total tests pass, 0 fail. (Was 541; +14 new = 555, +rest from other extensions = 566.)

STEP 4. Restart Streamlit and verify protective fixes still in place.

cd artifacts/survey-insight-engine && streamlit run app.py
sed -n '1620,1625p' app.py
grep -n "^def _run_pipeline\|app = _require_streamlit" app.py | head -8

Line ~1622 must show "with app.container():". _run_pipeline must have "app = _require_streamlit()" within its first non-import statements.

STEP 5. Browser smoke test — regenerate a WVL workbook.

In the browser:
- Hard-refresh
- BCN should still be loaded; re-run analysis if session was lost
- Define winner + laggard cohorts on an outcome question
- Click "Outcome Segmented Workbook (Winners vs Laggards)" download
- Open the new workbook in Excel/LibreOffice
- Visual check on Demographics sheet:
  - Bain red banner at the top? (Demographics in white on red fill)
  - Per-question filter row above each question? (Per-question filter | Filter Q | (None) | Value | (All))
  - Cross-tab row?
  - Sample-size note?
  - Counts display as "147" not "147.0"?
  - Percentages display as "23.5%" not "0.235"?
  - Delta % displays with sign like "+5.1%" or "-3.2%"?

REPORT BACK

- STEP 2 WVL test result
- STEP 3 full suite total
- STEP 4 startup status + protective check
- STEP 5 visual verification on the new workbook (just a yes/no per item)