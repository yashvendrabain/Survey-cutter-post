ROUND 5K.3 INSTALLATION — Critical decoder fix: gate option-mapping on question type so GRID_RATED and GRID_SINGLE_SELECT preserve raw values.

Codex shipped:
- src/raw_decoder.py (added pre-pass classifier + dispatch gate on question type)
- tests/test_raw_decoder_grid_preservation_real.py (new, 5 tests)
- 747 tests passing locally

The fix: a lightweight classifier pre-pass runs inside the decoder. For GRID_RATED columns, option-mapping is skipped entirely. For GRID_SINGLE_SELECT, only true text-selection columns get binary-encoded; numeric ratings pass through.

INSTRUCTIONS

STEP 0. Defensive pre-checks.

cd artifacts/survey-insight-engine && grep -n "_run_with_status_heartbeat" app.py | head -3
grep -n "def get_effective_values\|manual_cohort_id_column" src/models.py
grep -n "CONFIDENCE_REJECT_THRESHOLD\|min_questions" src/adapters/registry.py | head -5

All must return matches.

STEP 1. Install both files.

After install, verify the pre-pass landed:

grep -n "_question_types_for_decoding\|GRID_RATED\|GRID_SINGLE_SELECT" src/raw_decoder.py | head -15

Should show the pre-pass function + dispatch checks.

Verify the silent-failure path. The pre-pass swallows exceptions with `except Exception: return {}`. If the classifier crashes, the decoder falls back to old broken behavior. Show me the except block:

sed -n '500,512p' src/raw_decoder.py

If the except is `except Exception: return {}` without logging, that's a silent data corruption risk. Note it but don't block the install.

STEP 1.5. Path fix check.

grep "APP_PATH = Path" tests/test_raw_decoder_grid_preservation_real.py 2>/dev/null

Apply sed if needed.

STEP 2. Run new tests.

pytest tests/test_raw_decoder_grid_preservation_real.py -v 2>&1 | tail -25

Expected: 5 tests pass.

STEP 3. Run decoder + classifier regression tests.

pytest tests/test_raw_decoder.py tests/test_question_classifier.py tests/test_subcolumn_label_pattern.py tests/test_grid_categorical_row_adapter.py tests/test_grid_rated_double_colon_adapter.py -v 2>&1 | tail -30

Expected: pass.

STEP 4. Full suite chunked.

Expected: ~747.

STEP 5. Restart Streamlit, verify clean startup.

cd artifacts/survey-insight-engine && streamlit run app.py

The pre-pass classifier runs inside the decoder now. If classifier imports anything that wasn't safe at decode time, we'll see a startup failure. Watch for it.

STEP 6. CRITICAL — browser end-to-end on real winvslag2024.

Have user upload winvslag2024 → Run Analysis → Download workbook to outputs/.

Then verify:

cd artifacts/survey-insight-engine && python3 << 'EOF'
import openpyxl, glob, os
files = sorted(glob.glob('outputs/*.xlsx'), key=os.path.getmtime, reverse=True)
if not files:
    print("No workbook in outputs/")
else:
    path = files[0]
    print(f"Checking: {path}\n")
    wb = openpyxl.load_workbook(path, data_only=True)
    ws_raw = wb["_RawData"]
    headers = [ws_raw.cell(row=1, column=c).value for c in range(1, ws_raw.max_column + 1)]
    
    # Check Q27, Q97, Q14 decoded values
    test_cases = [
        ("Q27 Field_sales", lambda h: h and "Q27" in str(h) and "Field" in str(h)),
        ("Q97 Opportunity", lambda h: h and "Q97" in str(h) and "Opportunity" in str(h)),
        ("Q14 Actual Revenue", lambda h: h and "Q14" in str(h) and "Actual" in str(h) and "Revenue" in str(h)),
        ("Q13 Overall_company", lambda h: h and "Q13" in str(h) and "Overall" in str(h)),
    ]
    
    for label, matcher in test_cases:
        col_idx = None
        for i, h in enumerate(headers):
            if matcher(h):
                col_idx = i + 1
                break
        if not col_idx:
            print(f"{label}: column not found")
            continue
        val_counts = {}
        for r in range(2, ws_raw.max_row + 1):
            v = ws_raw.cell(row=r, column=col_idx).value
            key = f"{type(v).__name__}:{str(v)[:15]}"
            val_counts[key] = val_counts.get(key, 0) + 1
        top = sorted(val_counts.items(), key=lambda x: -x[1])[:5]
        print(f"{label}:")
        for k, count in top:
            print(f"  {k}: {count}")
        is_selected = any("Selected" in k for k in val_counts)
        is_numeric = any(k.startswith("int") or k.startswith("float") for k in val_counts)
        if is_selected:
            print(f"  >>> BUG STILL PRESENT — 'Selected' detected")
        elif is_numeric:
            print(f"  >>> FIX WORKED — numeric values preserved")
        print()
    
    # Check Q27 single-cut sheet
    print("="*70)
    for sn in wb.sheetnames:
        if sn.startswith("_") or sn in ("Run_Summary","Question_Metadata","Single_Cut_Index","Calculation_Log","Filter_Log","Warnings","Filters","Demographics"):
            continue
        ws = wb[sn]
        for r in range(1, min(ws.max_row + 1, 500)):
            a = ws.cell(row=r, column=1).value
            if isinstance(a, str) and a.startswith("Q27 -"):
                print(f"=== Q27 on {sn} starting row {r} ===")
                for offset in range(0, 20):
                    rr = r + offset
                    cells = [f"{ws.cell(row=rr, column=c).coordinate}={str(ws.cell(row=rr, column=c).value)[:20]}"
                             for c in range(1, 10)
                             if ws.cell(row=rr, column=c).value is not None]
                    if cells:
                        print(f"  R{rr}: {' | '.join(cells)}")
                    elif offset > 8:
                        break
                break
EOF

REPORT BACK
- STEPS 0-5 results
- STEP 6 output for all 4 questions
- If Q27/Q97 show numeric and Q14 shows float, FIX CONFIRMED