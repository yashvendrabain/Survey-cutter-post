Round 12 + 12-fix deploy. The cross-tab-by feature now works for every question type (grid-rated, rank via Net Rank Score, NPS, multi-select, numeric), plus a Net Rank Score column on rank single cuts and blanked Mean/Median on categorical questions. I've placed these Codex-changed files in the file tree (binary upload — do NOT open, reformat, or re-indent any of them):

  app.py
  src/excel_exporter.py
  src/cross_cut_engine.py
  src/single_cut/rank_order.py
  src/models.py
  src/filtered_single_cut.py
  tests/test_excel_exporter.py

Run exactly this in the shell and paste me the full output. Do not modify anything else.

cd ~/workspace/artifacts/survey-insight-engine

# 1. Indent repair (no-op if the binary upload was clean)
python3 - << 'EOF'
import ast
t=open("app.py",encoding="utf-8").read()
if t.startswith("\ufeff"): t=t[1:]
L=t.split("\n"); N=0
for ln in L:
    s=ln.lstrip(" ")
    if s.startswith("from __future__"): N=len(ln)-len(s); break
def st(ln,n):
    i=0
    while i<n and i<len(ln) and ln[i]==" ": i+=1
    return ln[i:]
f="\n".join(st(ln,N) for ln in L); ast.parse(f)
open("app.py","w",encoding="utf-8").write(f)
print(f"OK removed {N} sp/line, lines:", f.count(chr(10))+1)
EOF

# 2. Protective markers + Round 12 deltas
echo "heartbeat(6):     $(grep -c _run_with_status_heartbeat app.py)"
echo "threadpool(1):    $(grep -c 'ThreadPoolExecutor(max_workers=3)' app.py)"
echo "svg_icon(5):      $(grep -c _svg_icon app.py)"
echo "chevrons(2):      $(grep -c _render_wizard_top_nav app.py)"
echo "NPS render(1):    $(grep -c 'isinstance(result, NPSResult)' app.py)"
echo "filter_values:    $(grep -c filter_values src/models.py)"
echo "grid_col_labels:  $(grep -c grid_column_labels src/models.py)"
echo "net_rank_score:   $(grep -c net_rank_score src/models.py)"
echo "net_rank metric:  $(grep -c '\"net_rank\"' src/excel_exporter.py)"
echo "gridSS xtab fix:  $(grep -c GridSingleSelectResult src/excel_exporter.py)"
echo "VALUE in exporter (MUST be 0): $(grep -c 'VALUE(' src/excel_exporter.py)"
python -c "import src.excel_exporter, src.cross_cut_engine, src.filtered_single_cut, src.single_cut.rank_order, src.models; print('imports OK')"

# 3. Tests — 18 known pre-existing failures are expected; call out any NEW failure by name
python -m pytest -q 2>&1 | tail -8

# 4. Restart Streamlit and health-check
pkill -9 -f streamlit; sleep 3
nohup streamlit run app.py --server.port 21049 --server.address 0.0.0.0 --server.headless true > /tmp/streamlit.log 2>&1 &
disown; sleep 10
curl -s -o /dev/null -w "HTTP %{http_code}\n" localhost:21049/

Expected: indent "removed 0", markers 6 / 1 / 5 / 2 / 1, filter_values + grid_column_labels + net_rank_score all >=1, net_rank metric >=2, GridSingleSelectResult >=1, VALUE in exporter = 0, "imports OK", pytest with no NEW failures beyond the 18 known (test_chart_type_override x12, test_raw_decoder x4, and the 2 stale dropdown/metric-pair assertions), and HTTP 200. If anything deviates, stop and paste the output before proceeding.