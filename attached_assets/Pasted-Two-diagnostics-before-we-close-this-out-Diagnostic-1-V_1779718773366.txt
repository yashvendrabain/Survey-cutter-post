Two diagnostics before we close this out.
Diagnostic 1 — Validate the 216 → 124 schema reduction.
Run this and paste the output:
pythonfrom pathlib import Path
import pandas as pd
from src.io import load_inputs
from src.datamap_parser import parse_datamap
from src.question_classifier import classify_questions

raw_path = Path('sample_data/BCN_LTB_raw_data.xlsx')
map_path = Path('sample_data/BCN_LTB_datamap.xlsx')
decoded_df, data_map, _report = load_inputs(raw_path, map_path)
schema = classify_questions(data_map, set(decoded_df.columns))

# Sanity check: which parent IDs absorbed children?
from collections import defaultdict
parents_with_children = defaultdict(list)
for q in data_map['questions']:
    children = q.get('children', [])
    if children:
        parents_with_children[q['canonical_id']] = [c['canonical_id'] for c in children]

print(f"Parsed question count: {len(data_map['questions'])}")
print(f"Classified schema count: {len(schema.questions)}")
print(f"\nParents that absorbed children: {len(parents_with_children)}")
for parent, children in sorted(parents_with_children.items())[:15]:
    print(f"  {parent}: {len(children)} children ({children[:3]}...)")

# Spot-check: are any "missing" question IDs ones a consultant would expect to see?
# Print all Q-prefixed canonical_ids that exist in the schema
q_ids = sorted([q.canonical_id for q in schema.questions if q.canonical_id.startswith('Q')])
print(f"\nAll Q-prefixed IDs in schema ({len(q_ids)}):")
for qid in q_ids:
    print(f"  {qid}")
Diagnostic 2 — Q28 sanity check.
Run a full single-cut on Q28 to confirm it still computes correctly:
pythonfrom src.single_cut.engine import compute_single_cuts
from src.calculation_log import CalculationLog
from src.models import SurveySchema

q28_spec = schema.get_question("Q28")
print(f"Q28 spec: type={q28_spec.question_type if q28_spec else 'NOT IN SCHEMA'}")
if q28_spec:
    print(f"  analysis_eligible={q28_spec.analysis_eligible}")
    print(f"  conditional_on={q28_spec.conditional_on}")
    subset_schema = SurveySchema(
        questions=(q28_spec,),
        respondent_id_column=schema.respondent_id_column,
        total_respondents=schema.total_respondents,
        source_datamap_path=schema.source_datamap_path,
        source_rawdata_path=schema.source_rawdata_path,
        parsed_at=schema.parsed_at,
    )
    log = CalculationLog()
    results, skips = compute_single_cuts(subset_schema, decoded_df, log)
    print(f"\nQ28 results: {len(results)}, skips: {len(skips)}")
    for r in results:
        print(f"  Q28 valid_n={r.valid_n}, distribution count={len(r.distribution)}")
    for s in skips:
        print(f"  Q28 SKIPPED: {s.skip_reason}")
Paste both outputs. Don't take any other action.