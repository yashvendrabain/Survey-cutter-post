AUTHORIZED — Re-install the 6 hotfix files that were uploaded but never written to disk.

The previous install left src/io.py and src/models.py as their pre-hotfix versions. The detect_manual_cohort_sheet function has no two-pass fallback, _respondent_id_values resolves to "record" only, ManualCohortInput has no id_column field, and SegmentDefinition has no manual_cohort_id_column. All hotfix markers are missing on disk.

INSTRUCTIONS

STEP 0. Defensive checks on current production files BEFORE re-install.

cd artifacts/survey-insight-engine && grep -n "def get_effective_values" src/models.py
grep -n "label_to_numeric_value\|allowed_numeric_range" src/models.py
grep -n "laggard_values\|laggard_threshold_direction\|manual_winner_uuids" src/models.py

All must return matches.

STEP 0.5. Verify incoming hotfix files contain the hotfix markers.

grep -n "_manual_cohort_fallback_column\|id_column" attached_assets/io_*.py | head -10
grep -n "manual_cohort_id_column" attached_assets/models_*.py

The io.py upload should contain _manual_cohort_fallback_column and id_column references. The models.py upload should contain manual_cohort_id_column. If either is missing in the incoming file, STOP — Codex shipped the wrong file.

STEP 1. Re-install all 6 hotfix files.

After install, verify the markers landed:

grep -n "_manual_cohort_fallback_column" src/io.py
grep -n "id_column" src/io.py | head -10
grep -n "manual_cohort_id_column" src/models.py

All three commands must return matches. If any is empty, the file didn't get written — try cp directly from attached_assets:

cp attached_assets/io_*.py src/io.py
cp attached_assets/models_*.py src/models.py
cp attached_assets/outcome_segmentation_*.py src/outcome_segmentation.py
cp attached_assets/app_*.py app.py

Then re-run the marker greps.

STEP 2. Apply path fix to any new test files (recurring Codex bug).

grep "APP_PATH = Path" tests/test_manual_cohort_upload.py tests/test_app_manual_cohort_ui.py

If either has the doubled "artifacts/survey-insight-engine" pattern:

sed -i 's|parents\[1\] / "artifacts" / "survey-insight-engine" / "app.py"|parents[1] / "app.py"|' tests/test_manual_cohort_upload.py tests/test_app_manual_cohort_ui.py

STEP 3. Run hotfix-affected tests.

pytest tests/test_manual_cohort_upload.py tests/test_app_manual_cohort_ui.py -v 2>&1 | tail -30

Expected: all pass.

STEP 4. Full suite chunked.

Expected: ~620 total, 0 failures.

STEP 5. Verify protective fixes in app.py.

sed -n '1625,1635p' app.py
grep -n "^def _run_pipeline\|app = _require_streamlit" app.py | head -5

`with app.container():` must be present in the chart think-cell section. _run_pipeline must have `app = _require_streamlit()` early in its body.

STEP 6. Restart Streamlit.

cd artifacts/survey-insight-engine && streamlit run app.py

Verify clean startup.

STEP 7. Browser smoke test — the actual fix verification.

Hard-refresh browser. Upload survey_file_win_vs_lag_sample_input.xlsx as a combined upload (NOT separate uploader — the embedded path is what we're testing).

Expected behavior:
- Manual cohort detected badge: "256 winners, 216 laggards from uploaded file" (NOT "0 winners, 0 laggards")
- Possibly an app.info message: "Manual cohort matched against 'uuid' column..."
- Summary panel: Winners 256 (256 valid, 1 not found = the stray "0"), Laggards 216 (216 valid, 0 not found), 0 overlap

If badge still says "0 winners, 0 laggards" after this re-install:
(a) Check `grep -n "_manual_cohort_fallback_column" src/io.py` — function MUST exist
(b) Check `grep "respondent_id_column=\"record\"" src/io.py` — this hardcoded line MUST be replaced with the fallback-calling version
(c) Restart Streamlit again (sometimes module imports cache the old version)

STEP 8. Verify on-disk markers post-install.

grep -nE "_manual_cohort_fallback_column|id_column=|manual_cohort_id_column" src/io.py src/models.py src/outcome_segmentation.py | head -20

Should return multiple matches across all three files.

REPORT BACK

- STEP 0 / 0.5 marker checks (incoming files have the hotfix markers, current files don't)
- STEP 1 post-install verification — which markers landed?
- STEP 3 test result
- STEP 4 full suite total
- STEP 5 protective check
- STEP 7 browser test result (256/216 winners/laggards, NOT 0/0)
- STEP 8 final marker verification

If the markers STILL don't land after explicit cp commands, Replit's file system or the IDE is preventing the write. In that case paste the exact error or status from each cp command.