Apply Codex's updated src/question_classifier.py. No other files change.
Run the 5-step verification you outlined:

Test suite:
bashpython -m pytest tests/ -v 2>&1 | tail -15
Expected: 467 passing in your sandbox (Codex has 464; you have 3 extra FilterSpec multi-value tests).
Equality invariant — every spec's question_id matches its canonical_id:
pythonfrom pathlib import Path
from src.io import load_survey_inputs
from src.question_classifier import classify_questions

decoded_df, data_map, _ = load_survey_inputs(Path('sample_data/BCN_combined.xlsx'))
schema = classify_questions(data_map, set(decoded_df.columns))

mismatches = [(q.question_id, q.canonical_id) for q in schema.questions if q.question_id != q.canonical_id]
print(f"Mismatches: {len(mismatches)} (should be 0)")
for qid, cid in mismatches[:5]:
    print(f"  {qid!r} != {cid!r}")
Expected: 0 mismatches.
All 14 previously-broken IDs now resolve:
pythonfor qid in ["Q11", "Q13", "Q14", "Q15", "Q17", "Q18", "Q23", "Q24", "Q28", "Q29", "Q31", "Q32", "Q39", "Q42"]:
    spec = schema.get_question(qid)
    ok = spec is not None
    print(f"  {qid}: {'✓' if ok else '✗ NONE'} type={spec.question_type.value if spec else 'N/A'}")
Expected: all 14 resolve to a real spec.
Re-run the 9 spot-check engine subset. Confirm the previously-passing checks still pass:

Q26/Q41 column_headers have role names, not "Checked"/"c2"
Q43 valid_n ≤ 638
Q22/Q34/Q35 have conditional_on=None or a valid ID (no bogus tokens)


Export a workbook from the BCN data and verify question text headers. This is the critical user-visible test that proves the bug is dead. Export any subset of 4-5 questions including Q28, Q11, Q18, Q43, and Q26. Open the workbook. For each, paste the literal text of the question header cell.
Expected: real question wording, e.g. for Q28 the header should say "Which vendor did your organization ultimately purchase from for [pipe: H_Q9] software?" — NOT "[Q28]". Even if pipe markers like [pipe: H_Q9] remain in the text, that's a separate cosmetic concern; what we're checking is that the actual question wording appears, not a placeholder.

Paste back results from all 5 steps. If everything's clean, the classifier overhaul is officially shipped.