# Replit Diagnostic — Find why 13+ questions are missing from the schema

We need to know what's happening to Q11, Q13, Q14, Q15, Q17, Q18, Q23, Q24, Q28, Q29, Q31, Q32, Q37, Q39, Q42 between the raw datamap and the parsed data_map. These IDs exist in the BCN raw data columns but are missing from the parsed schema.

Run this diagnostic and paste the output. Do not fix anything.

```python
from pathlib import Path
import openpyxl
import pandas as pd
import re
from src.io import load_inputs
from src.datamap_parser import parse_datamap

raw_path = Path('sample_data/BCN_LTB_raw_data.xlsx')
map_path = Path('sample_data/BCN_LTB_datamap.xlsx')

# === Step 1: Load raw datamap directly with openpyxl and find Q28, Q18, Q29 blocks ===
print("=" * 80)
print("RAW DATAMAP INSPECTION — looking for missing questions")
print("=" * 80)

wb = openpyxl.load_workbook(map_path, read_only=True, data_only=True)
ws = wb.active
all_rows = list(ws.iter_rows(values_only=True))

missing_ids = ["Q11", "Q13", "Q14", "Q15", "Q17", "Q18", "Q23", "Q24", "Q28", "Q29", "Q31", "Q32", "Q37", "Q39", "Q42"]

for target in missing_ids:
    print(f"\n--- {target} ---")
    found_any = False
    for i, r in enumerate(all_rows):
        if r[0] and isinstance(r[0], str):
            cell = r[0].strip()
            # Match Qxx: or [Qxx] or [Qxxr1] header rows
            m = re.match(r'^\[?(' + re.escape(target) + r'(?:r\d+|c\d+|_\w+)?)\]?[\s:.]', cell)
            if m:
                found_any = True
                print(f"  R{i+1}: starts with {m.group(1)!r}: {cell[:120]}")
                # print next 5 rows to see structure
                for j in range(i+1, min(i+6, len(all_rows))):
                    next_row = all_rows[j]
                    cells = [str(c) for c in next_row if c is not None]
                    if cells:
                        print(f"    R{j+1}: {next_row[:5]}")
                break
    if not found_any:
        print(f"  NOT FOUND in datamap")

# === Step 2: Run the parser and see what survived ===
print("\n" + "=" * 80)
print("PARSER OUTPUT — what survived parsing")
print("=" * 80)

decoded_df, data_map, _report = load_inputs(raw_path, map_path)

print(f"\nTotal parsed: {len(data_map['questions'])}")

# Get all canonical_ids that exist
canonical_ids_in_dm = sorted({q['canonical_id'] for q in data_map['questions']})

# For each missing target, check if ANY trace of it exists
print(f"\nTraces of missing targets in parsed data_map:")
for target in missing_ids:
    matches = [cid for cid in canonical_ids_in_dm if cid == target or cid.startswith(target + 'r') or cid.startswith(target + 'c') or cid.startswith(target + '_')]
    if matches:
        print(f"  {target}: {matches}")
    else:
        print(f"  {target}: NO TRACE")

# === Step 3: For Q28 specifically, check raw data column presence ===
print("\n" + "=" * 80)
print("RAW DATA COLUMN PRESENCE")
print("=" * 80)

for target in missing_ids:
    cols_in_raw = [c for c in decoded_df.columns if c == target or c.startswith(target + 'r') or c.startswith(target + 'c') or c.startswith(target + '_')]
    if cols_in_raw:
        # Show non-null count for first column
        first = cols_in_raw[0]
        non_null = decoded_df[first].notna().sum()
        print(f"  {target}: {len(cols_in_raw)} cols in raw data; {first} has {non_null} non-null")
    else:
        print(f"  {target}: 0 cols in raw data")

# === Step 4: Show what type_hint and value_range got captured for Q28 if it exists at all ===
print("\n" + "=" * 80)
print("FULL PARSED RECORD for Q28 (if any)")
print("=" * 80)
for q in data_map['questions']:
    if 'Q28' in str(q.get('canonical_id', '')) or 'Q28' in str(q.get('question_text', '')):
        print(f"  canonical_id={q.get('canonical_id')}")
        print(f"  raw_id={q.get('raw_id')}")
        print(f"  type_hint={q.get('type_hint')}")
        print(f"  value_range={q.get('value_range')}")
        print(f"  options={q.get('options', [])[:5]}")
        print(f"  question_text={q.get('question_text', '')[:100]}")
        print(f"  warnings={q.get('warnings', [])}")
        print()
```

After you run this, paste the entire output. Three pieces of information will tell us exactly what's wrong:

1. **Raw datamap inspection** — does Q28 (and the others) exist as a `Q28:` block in the datamap? If yes, the parser dropped them. If no, the datamap itself is missing them (which would be very strange but possible).
2. **Parser output** — confirms the parser is producing the smaller list and lets us see exactly which IDs are gone.
3. **Raw data column presence** — confirms the columns exist in the raw data (so the questions are real, just missing from the schema).

Once we see the output, I can write a targeted Codex prompt — or, if the datamap itself is the problem, tell you that no code change is needed and the issue is upstream.

Do not fix anything. Do not assume the parser is the bug. The output will tell us what to do.