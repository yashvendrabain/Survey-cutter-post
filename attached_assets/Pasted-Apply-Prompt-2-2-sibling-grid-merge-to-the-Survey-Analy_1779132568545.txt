Apply Prompt 2.2 (sibling-grid merge) to the Survey Analysis Engine.
Files to replace from Codex:

src/question_classifier.py — adds _merge_grid_siblings post-pass that merges Q26r1...Q26rN style sibling specs into one parent spec Q26. Skips GRID_BINARY_SELECT (preserves Q38).
src/excel_exporter.py — Delta values now rounded to 1 decimal at formula time.
tests/test_question_classifier.py + tests/test_excel_exporter.py — adds regression tests for sibling merge, separate families, negative cases, Q38 non-merge, end-to-end rendering.

What this iteration ships:

BCN's Q26, Q30, Q36 (and any survey with the <parent>r<N> family naming convention sharing a question text root) will now render as a SINGLE consolidated block per parent question.
Q38 and other binary-select grids are excluded from merging — preserves existing rendering.
Delta values no longer show floating-point artifacts.

Heads-up on Codex's recurring regressions — pre-empt before tests:

parents[1] / "artifacts" / "survey-insight-engine" / "app.py" → parents[1] / "app.py"
Stale AI-label assertions ("...Export Question" → "...Export")
LONG_ID exact match → startswith prefix

Run after replacing:

Apply Codex regressions if present.
pytest full suite. Codex reported 403 passes. Confirm exact count.
Generate small sample workbook (outputs/survey_prompt2_2_sample.xlsx). Verify renders cleanly.
Generate BCN workbook (outputs/survey_prompt2_2_bcn.xlsx) via the same streaming-export workflow path as previous BCN runs.

Verify on the BCN workbook:

Q26 renders as a SINGLE block. Sub-question rows show stakeholder labels (IT/Technical, Security, Risk, Compliance, Finance...). Columns show role values (Decision maker, Influencer, etc.) with count cells.
Q30 renders as a SINGLE block. Sub-question rows show criterion labels (Pre-purchase familiarity, Customer validation...). Columns grouped as Winner - All | Other considered vendor | Delta, each with Mean | Median sub-columns.
Q36 renders as a SINGLE block, same format as Q30.
Q38 renders as before — multiple sibling blocks each with binary selected/not-selected counts. NOT merged.
Delta values show one decimal place, no floating-point artifacts.
Cross-tab heatmap still works (regression check).

Report:

Test count
Pass/fail on each of the 6 verification items
Heading count per parent (e.g., Q26: 1, Q30: 1, Q36: 1, Q38: N>1)
One screenshot of Q30 on the BCN sheet (the most important verification — that's what the grid renderer was designed for)

If anything fails or renders unexpectedly, paste the relevant rows from the BCN workbook so we can diagnose. Do not patch on your own. The architecture is delicate and patches without diagnosis have caused regressions before.