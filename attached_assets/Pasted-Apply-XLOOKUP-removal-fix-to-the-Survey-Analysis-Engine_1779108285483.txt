Apply XLOOKUP removal fix to the Survey Analysis Engine.
Files to replace from Codex:

src/excel_exporter.py — replaces every XLOOKUP with IFERROR(INDEX(..., MATCH(...)), fallback) to avoid openpyxl emitting _xludf.XLOOKUP which Excel can't resolve.
tests/test_excel_exporter.py — regression tests asserting no XLOOKUP/_xludf in generated workbooks and no #NAME? cached values.

Root cause that was fixed: openpyxl writes XLOOKUP calls as _xludf.XLOOKUP because it doesn't recognize XLOOKUP as a built-in function. Excel reads _xludf.XLOOKUP and produces #NAME? errors → per-question filter Value dropdowns showed empty, cross-tab columns stayed blank, custom filter resolved-column formulas errored.
Verified in claude review:

0 XLOOKUP occurrences in generated worksheet XML
0 #NAME? cached values
INDEX/MATCH formulas resolve to real range strings as expected

Heads-up on Codex's recurring regressions — pre-emptively check:

parents[1] / "artifacts" / "survey-insight-engine" / "app.py" in any new test → should be parents[1] / "app.py"
Stale AI-label assertions ("Single/Multi/Numeric Export Question" → trimmed to "Single/Multi/Numeric Export")
LONG_ID exact assertion → should be startswith(f"{LONG_ID} - ") prefix match

Run after replacing:

Apply Codex's known regressions if present, then pytest. Target: 391+. Report exact count.
Smoke-test the small sample workbook through the Streamlit app:

Open generated workbook in Excel
On any theme sheet, set Filter Q on a per-question filter row → click the Value dropdown → confirm it shows the chosen question's actual options (not empty, not (All) only).
On any block, set Cross-tab by to a real question → confirm column headers populate with the cross-tab values and counts/% appear in the 2D grid (not all 0.0% or blank).
On Filters sheet, set Custom 1 Q to a real question → click Custom 1 V → confirm dropdown populates.



Report all three checks pass/fail.
After this lands we move to the BCN UI issues (Download button disabled, in-app filter not updating) per Sequence A.