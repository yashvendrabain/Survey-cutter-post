ROLLBACK FIRST, then add the nav bar. Last response only did step 2 
(nav bar) and skipped step 1 (revert). Do the revert now.

══════════════════════════════════════════════════════════════════════
DIAGNOSIS REQUIRED FIRST
══════════════════════════════════════════════════════════════════════

Before changing anything, find git history or backups of app.py from 
end of Stage D (before the UI revamp work began this session).

Run these commands and paste output:

1. git log --oneline -20 artifacts/survey-insight-engine/app.py
2. ls -la artifacts/survey-insight-engine/*.bak 2>/dev/null
3. ls -la /tmp/*.py 2>/dev/null
4. grep -n "_render_left_sidebar\|_render_question_navigator\|_inject_theme_css_DAY\|_THEME_CSS_DAY" artifacts/survey-insight-engine/app.py | head -20

The old UI included:
- Full red fixed header at top (NOT a thin strip)
- Left sidebar with question navigator (browseable list of all 
  questions in the survey, with search box)
- Section-by-section page layout
- Original CSS theme

Report what you find. Specifically:
- Most recent commit BEFORE the UI revamp started
- Any .bak or backup files of app.py
- Functions related to the old sidebar/header that still exist in 
  current app.py (even if not called)

══════════════════════════════════════════════════════════════════════
ROLLBACK PROCEDURE
══════════════════════════════════════════════════════════════════════

Based on what you find:

OPTION A — If git history exists:
  Find the commit at end of Stage D 
  (search for commit messages around "Stage D" or "AI analysis" or 
  "cross cut suggestions", BEFORE any mention of "UI revamp", 
  "brand header", or "sidebar nav").
  
  Run: git log --all --oneline --grep="Stage D\|cross.cut.suggest" | head
  
  Then:
  git show <commit_hash>:artifacts/survey-insight-engine/app.py > /tmp/app_stageD.py
  
  Compare to current app.py:
  diff /tmp/app_stageD.py artifacts/survey-insight-engine/app.py | head -100
  
  Paste the diff output. Do NOT apply yet.

OPTION B — If git unavailable but functions still exist:
  Check if the original functions are still present as "dead code" 
  in current app.py:
  
  grep -n "^def _render_sidebar\|^def _inject_theme_css\|^def _render_question_navigator\|^def _render_header\|^def _section_header" artifacts/survey-insight-engine/app.py
  
  Report which old functions still exist.

OPTION C — If neither:
  Search /home, /tmp, /workspace for any older copy of app.py:
  find / -name "app.py" -path "*survey*" 2>/dev/null
  
  Paste output.

══════════════════════════════════════════════════════════════════════
DO NOT MAKE ANY CHANGES YET
══════════════════════════════════════════════════════════════════════

Just gather and report the diagnostic info. Once I see what's 
recoverable, I'll give the precise revert instructions.

The functions/elements to look for that need to come BACK:
1. Full red header banner (the prominent fixed one, not the thin strip)
2. Left sidebar with question navigator (search + browseable list)  
3. Original theme CSS (white background, Arial, red accents)
4. Original page-section flow with dividers

DO NOT keep:
1. The brand-header thin strip we added
2. The sb-item / sb-section / sb-runlog sidebar classes
3. The workflow steps radio in sidebar
4. _render_brand_header function
5. _render_sidebar function (the new workflow one)

But KEEP these recent improvements:
1. ai_insights.py with table/outlier functions + validation
2. _render_outcome_summary_panel with key insight + outlier + table
3. _render_insight_card with red number highlighting
4. The new nav bar function (will integrate after revert)

Report findings. Do not change code yet.