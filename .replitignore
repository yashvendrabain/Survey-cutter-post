# The format of this file is identical to `.dockerignore`.
# It is used to reduce the size of deployed images to make the process of publishing faster.

# No need to store the pnpm store twice.
.local

# Large generated download archive (multi-GB) — not needed at runtime.
dist

# Package/build caches — not needed at runtime.
.cache

# Git history — not needed in the deployed image.
.git

# Uploaded source assets (originals, spreadsheets, prior app versions) — not needed at runtime.
attached_assets

# Survey app: test fixtures, scratch outputs, and editor backups — not needed at runtime.
artifacts/survey-insight-engine/tests
artifacts/survey-insight-engine/outputs
artifacts/survey-insight-engine/.pytest_cache
**/*.bak
**/*.bak_*

# Python env: `uv sync` rebuilds this fresh as a proper venv during the deploy
# build. The dev .pythonlibs is a non-venv --user tree whose `bin/python`
# symlinks into the READ-ONLY nix store; shipping it makes the deploy's
# `uv sync` resolve that interpreter and try to install into the nix store,
# failing with "Permission denied (os error 13)". Excluding it lets the build
# create a clean, writable venv at .pythonlibs (also trims ~719MB from image).
.pythonlibs
