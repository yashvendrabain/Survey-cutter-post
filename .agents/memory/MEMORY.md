- [Streamlit websocket idle timeout](streamlit-websocket-idle-timeout.md) — long (>~30s) synchronous Streamlit runs silently fail on Replit (proxy drops idle socket); fix with status.update heartbeat in a worker thread.
- [Streamlit orphan blocks restart](streamlit-orphan-port-restart-fail.md) — restart_workflow fails TASK_FAILED when a stale hours-old streamlit still holds the port; kill the orphan, then restart (it also serves stale src/ from sys.modules cache).
- [Codex test doubled APP_PATH](codex-test-doubled-app-path.md) — Codex test files sometimes set APP_PATH with a redundant `artifacts/survey-insight-engine` segment → FileNotFoundError; correct form is `parents[1] / "app.py"`. Check ALL new test files, not just the authorized sed glob.
- [Adapter sheet lookup & wizard gating](wizard-threshold-must-match-parse-floor.md) — survey-insight-engine: real wizard-fallback cause was hardcoded "Sheet1" data-map lookup (fix: _find_datamap_sheet by keyword); the 0.3 vs 0.5 threshold was a red herring since adapter scores never land in 0.3–0.5.
- [Grid single-select numeric decoding](grid-single-select-numeric-decoding.md) — survey-insight-engine: GRID_SINGLE_SELECT (values_range+sub_columns+options) must skip option-label mapping; numeric coercion runs BEFORE grid text-detection so mixed numeric+text cols silently drop text to NaN.
- [Real-file gate loader signature](realfile-gate-loader-signature.md) — survey-insight-engine: deploy-runbook verify scripts must wrap the workbook path in an UploadedFile-like adapter (name/read/seek) and pass a list; a bare path string raises AttributeError.
- [Excel named-range collisions](excel-named-range-collisions.md) — survey-insight-engine exporter: header-derived names need rstrip('_') to avoid double-underscore #NAME?; latent gap = rstrip+truncate can collapse 2 headers → silent overwrite (wrong binding, no #NAME?), validator won't catch.
- [Deployment image size & .replitignore](deployment-image-size-replitignore.md) — publish failing with "image size over 8 GiB" is a packaging issue, not code; .gitignore does NOT trim the image, only .replitignore does (e.g. a multi-GB dist/ zip).
